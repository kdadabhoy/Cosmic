# AP-00 — Write the packet, fix KI-39, supersede WO-11/12/13

**Gate:** G0 · **Wave:** 0 · **Runs:** `main` (planning session) · **Depends on:** — · **Acceptance:** both
audits exit 0 · **Model:** Fable 5.1 · **Status:** ✅ done 2026-09-18 (uncommitted at hand-off; Kaden
commits or the AP-05A session commits it first as its base)

The planning session produced this packet and the two small repo fixes below. It is recorded here so
the run order has a complete history; nothing in it needs re-running.

## What was done

- `tests/WO07PlotFixture.cpp:148-154` and `tests/WO07UiCyclesFixture.cpp:83`: the eight inline
  `/*GL_…*/` comments became `/*…*/` without the `GL_` prefix (the GL conformance audit matches
  `\bGL_[A-Z0-9_]+\b` even inside trailing comments — `tests/check_gl_conformance.ps1:29,34`).
- `docs/reference/README.md`: two manifest rows added — `serial/ISerialTransport.h → serial-telemetry.md`,
  `utils/AtomicOutput.h → assets-io.md` (the two headers KI-39 named as unlisted).
- Verified locally: `check_gl_conformance.ps1` → `GL conformance: clean`, exit 0;
  `check_docs_coverage.ps1` → `Docs coverage: clean (151 public headers, 148 manifest rows, …)`, exit 0.
  These are the two CI steps that failed on `main` at `8da533c` in 14 s, so the next push goes green.
- `docs/plans/2d-stability-2026-09-16/work-orders/{WO-11,WO-12,WO-13}.md`: SUPERSEDED banner at line 2;
  `…/work-orders/README.md`: "Campaign closed" section.
- This packet: `00-Start-Here.md`, `01-Design-Contracts.md`, `02-Work-Orders.md`,
  `03-Acceptance-Catalog.md`, `work-orders/README.md`, `work-orders/AP-*.md`, `evidence/README.md`.

## Commit (for whoever commits it)

Author `kdadabhoy <kdadabhoy28@gmail.com>`, no AI trailer, message:
`Add the App Platform planning packet, close KI-39 (CI audits green) and supersede WO-11/12/13 (AP-00)`.
The KI register entry KI-39 gets `Disposition: fix landed <sha>` in the same commit or the AP-05A one.

## Rollback

Docs + two comment edits + two manifest rows: revert the commit.
