# AP-05 — Purge every 3D remnant from `main` (Part A: delete · Part B: unfence)

**Gate:** G1 · **Wave:** 1 · **Runs:** `main`, alone, in `C:\dev\Cosmic` · **Base:** the AP-00 commit ·
**Depends on:** AP-00 · **Acceptance:** B06 · **Model:** Fable 5.1 for both parts · **Status:** not started

Two sessions, two commits. Part A removes what the 2D build already excludes (trees, files, vendored
deps, tests, goldens, editor TUs, template scripts, scripts/presets, manifest rows, the checker's 3D
logic). Part B removes the `COSMIC_2D_ONLY` fences from the 405 sites in 85 files with a nesting-aware
script and a reviewed diff, keeping the CMake option as an always-ON compatibility no-op. Everything
deleted stays on `engine-3d` (`0e8894b`) and the `cosmic-pre-2d-2026-09-16` tag — never touch those.

## Copy-paste prompt — Part A

~~~text
Execute only AP-05 Part A from the Cosmic App Platform packet (docs/plans/app-platform-2026-09-18/).
Read work-orders/README.md (all), then ONLY 01-Design-Contracts.md section 9 (purge scope) and section
14 (anchors). Work alone on main in C:\dev\Cosmic; nobody else edits this tree while you run.

Revalidate first: git rev-parse HEAD, git status --short (the untracked root file "Cosmic - 2D Trunk
Consolidation & Acceptance Plan.md" and recordings/ stay untouched), the toolchain. If AP-00's changes
are still uncommitted, commit them first exactly as work-orders/AP-00.md says (author kdadabhoy, no
trailer). Configure and build Debug with -DCOSMIC_2D_ONLY=ON -DCOSMIC_BUILD_TESTS=ON, run
CosmicTests --count and record the number as the pre-purge baseline; hash tests/render/goldens/*.png.

Delete exactly the section-9 Part A groups. Before deleting each vendored dependency
(recastnavigation, assimp, cgltf) grep Cosmic/src, Projects, tests for any 2D consumer and stop if one
exists. Remove the if(COSMIC_2D_ONLY) list(FILTER) block and the if(NOT COSMIC_2D_ONLY) dependency
blocks from Cosmic/CMakeLists.txt; remove the 3D-only TU block from tests/CMakeLists.txt and
render_3d.cpp from tests/render/CMakeLists.txt; remove the already-excluded editor TUs and their
exclusion lines from Projects/Starforge/CMakeLists.txt; delete VoxelDigger.h/NavCritter.h, their fenced
blocks in the template Module.cpp and the NavCritter include/test in tests/test_template_scripts.cpp;
delete build_3d.bat and the 3D preset (make "2d" the default preset, keep its name). Delete every
docs/reference/README.md row whose header no longer exists (do NOT touch any other row; do not move
chapter files — AP-D1 does that). Edit tests/check_docs_coverage.ps1: remove the CMake-filter parse
(it exits 1 when no filter rules exist), the fence-aware Cosmic.h parse and the 3D marker modes; keep
every other failure mode. Keep option(COSMIC_2D_ONLY ... ON), the reject-OFF gate and the PUBLIC
define untouched in Part A.

Rebuild Debug and Release from a clean configure; 0 warnings. Run CosmicTests both configs and record
the new --count; the difference from the baseline must equal the cases of the deleted TUs (list them).
Run the GL audit and the docs-coverage audit; both must exit 0. Hash the goldens again: every retained
2D golden byte-identical. Write tests/acceptance/fixtures/Verify-AP05Purge.ps1 (the B06 grep/file
oracles, parameterised on the Part-A path list, PowerShell 5.1, ASCII) and a manifest
tests/acceptance/manifests/ap05-purge.manifest.json with one U case running it; run it through
tests/acceptance/Run-Acceptance.ps1. Part A's oracle may still see preprocessor uses of COSMIC_2D_ONLY
(Part B removes them) — the script takes -AllowFences for Part A and the manifest passes it.

Evidence: docs/plans/app-platform-2026-09-18/evidence/AP-05/report.md (WO-10 layout) with the
baseline and post counts, the deleted-path list, the dependency-consumer greps, both audit outputs
and the golden hashes. Append KI-39 "fix landed <AP-00 sha>" to the KI register if AP-00 did not.
Commit locally as kdadabhoy with no AI trailer: "Purge the filtered 3D source, deps, tests, goldens and
editor TUs from main (AP-05 part A)". Do not push. Return: the two counts, the deleted-path list, the
audit results, and any file you could not delete and why.
~~~

## Copy-paste prompt — Part B

~~~text
Execute only AP-05 Part B from the Cosmic App Platform packet (docs/plans/app-platform-2026-09-18/).
Read work-orders/README.md (all), then ONLY 01-Design-Contracts.md section 9 "Part B" and the B06
oracle, and evidence/AP-05/report.md from Part A. Work alone on main in C:\dev\Cosmic on top of the
Part A commit.

Grep every spelling first and record the counts: "#ifndef COSMIC_2D_ONLY", "#ifdef COSMIC_2D_ONLY",
"#if defined(COSMIC_2D_ONLY)", "#if !defined(COSMIC_2D_ONLY)", "#elif" lines mentioning it, and
COSMIC_2D_ONLY inside compound conditions. Write evidence/AP-05/unfence.py (Python via the "py -3"
launcher; if no Python exists on this machine — "python" is a Store alias here — write the same logic
as evidence/AP-05/unfence.ps1 in PowerShell 5.1 instead): for each file under
Cosmic/src, Projects, tests, Cosmic/templates containing COSMIC_2D_ONLY, walk the lines with a
preprocessor-depth stack; on a marked #ifndef/#if !defined block drop the true branch and keep the
#else branch (if any); on a marked #ifdef/#if defined block keep the body; nested unrelated #if blocks
inside a dropped branch go with it; any #elif on a marked block, or COSMIC_2D_ONLY inside a compound
condition, is left untouched and printed for manual editing. The script prints per-file counts and a
dry-run diff stat before writing; run it, then review the diff FILE BY FILE (git diff --stat, then
each file's hunks) and hand-edit the listed exceptions. Edit the CMake if(COSMIC_2D_ONLY)/if(NOT ...)
blocks by hand (root CMakeLists.txt, Cosmic/CMakeLists.txt, tests, Projects). Keep option(COSMIC_2D_ONLY
... ON), the reject-OFF FATAL_ERROR gate and the PUBLIC compile definition as a compatibility no-op with
a comment saying so; keep every project CMakeLists' option(COSMIC_2D_ONLY ON) and BuildRunner's
-DCOSMIC_2D_ONLY=ON (harmless, keeps external projects configuring). Remove the fences from
tests/render/render_main.cpp and the template/ExampleProject sources too.

Rebuild Debug and Release from a clean configure; 0 warnings. Run CosmicTests both configs: the count
must equal Part A's post count. Run the GL and docs-coverage audits (exit 0). Re-hash the goldens
(byte-identical). Run tests/acceptance/manifests/ap05-purge.manifest.json WITHOUT -AllowFences (edit the
manifest): B06 must PASS. Also run the retained stability manifests wo07-l01, wo08-gpu (if a GPU is
present; otherwise ENVIRONMENT_BLOCKED), wo09-units, wo10-units to prove nothing regressed.

Report in evidence/AP-05/report.md: the counts before/after, the exception list and how each was
resolved, a "now-dead 3D-only classes" list (types that survive only because they were never fenced —
e.g. orbit/fly camera-rig parts, PoseCamera perspective paths — with file:line, for AP-Q1 to tidy), and
the retained-suite results. Commit locally as kdadabhoy with no AI trailer: "Remove the COSMIC_2D_ONLY
fences; the trunk is 2D-only source (AP-05 part B)". Do not push. Return: counts, exceptions, the
dead-class list, and the B06 result.
~~~

## Files to read first (and nothing else)

`work-orders/README.md`, `01-Design-Contracts.md` §9 + §14, `Cosmic/CMakeLists.txt`,
`tests/CMakeLists.txt`, `tests/check_docs_coverage.ps1` (Part A); Part A's report + `unfence.py` output
(Part B). Everything else is discovered by grep.

## Owns / May touch

Owns: every path in §9; `Cosmic/CMakeLists.txt`; root `CMakeLists.txt`; `tests/CMakeLists.txt`;
`tests/render/CMakeLists.txt`; `Projects/Starforge/CMakeLists.txt`; `CMakePresets.json`; `build_*.bat`;
`tests/check_docs_coverage.ps1`; `docs/reference/README.md` (row deletions only); fence sites in
`Projects/Starforge/src/**`, `Cosmic/templates/ExampleProject/**`, `tests/**`;
`tests/acceptance/fixtures/Verify-AP05Purge.ps1`; `tests/acceptance/manifests/ap05-*.json`;
`evidence/AP-05/**`. May touch: nothing else. **Never** `.github/workflows/**` (AP-P1), never
`engine-3d`, never the tag.

## Scope

- **In:** deletion of the excluded 3D surface and its fences; CMake/preset/script cleanup; manifest
  row deletions; checker simplification; the B06 oracle script.
- **Out:** moving or rewriting docs (AP-D1); new features; deleting dead-but-compiled classes (listed
  for AP-Q1); touching physics/Jolt (dimension-agnostic, stays).

## Deliverables

Two commits; `Verify-AP05Purge.ps1` + `ap05-purge.manifest.json`; `evidence/AP-05/{report.md,unfence.py}`.

## Done when (DoD)

B06 passes; both configs 0-warn; test counts reconcile; retained 2D goldens byte-identical; both audits
exit 0; the retained manifests listed pass or are honestly blocked; the dead-class list exists.

## Rollback

Revert the Part B commit, then the Part A commit (each is independent and self-contained).
