# AP-04 — Template kinds, samples on disk, PendulumLab, headless tests

**Gate:** G2 · **Wave:** 2 (worktree, alongside AP-02 and AP-D1) · **Runs:** worktree `ap/04` ·
**Base:** `main` after AP-P1 has landed · **Depends on:** AP-01 (via main); AP-02's component names
from the contract (not its code) · **Acceptance:** F02, Y01 (Y02/Y03 hosts built here, executed by
AP-Q1) · **Model:** Opus 5 · **Effort:** high · **Status:** not started

Content, not editor code: the `app` / `game` / `blank` templates and the two samples as on-disk
trees, plus the PendulumLab showcase project and the headless tests that keep every template script
and the sample's physics honest. Scenes are authored as JSON with the contract's reflected names and
validated by loading them through `SceneSerializer`; they render fully only once AP-02 lands, which is
why AP-03 (not you) runs the on-screen checks.

## Copy-paste prompt

~~~text
Execute only AP-04 from the Cosmic App Platform packet (docs/plans/app-platform-2026-09-18/). Read
work-orders/README.md (all, lane rules), then ONLY 01-Design-Contracts.md sections 2 (AppService API),
3 (component names/fields — you author scenes that use them), 5 (screen convention, stub shape,
Module.cpp markers, flow additions, manifest key), 8 (template layout) and 03-Acceptance-Catalog.md
rows F02, Y01, Y02, Y03 and the F-PENDULUM fixture.

Lane setup: from C:\dev\Cosmic run  git worktree add ..\Cosmic-ap-04 -b ap/04 main  (main contains
AP-01 and AP-P1), work inside C:\dev\Cosmic-ap-04, set $env:COSMIC_SDK to it, build there. AP-02 and
AP-D1 run concurrently; you never touch Cosmic/src, Starforge source or docs.

Do, in order:
1. Capture the two code-generated samples BEFORE anything else: build the editor in this worktree,
   run Starforge.exe, press "Flow Sample" and "Pong Sample" on the homescreen (or call the builders
   through a tiny env-gated hook if the UI is awkward — remove the hook afterwards), then copy
   build/Runtime/<cfg>/assets/projects/FlowDemo and ForgePong into Projects/Starforge/assets/templates/
   samples/{FlowDemo,ForgePong}/, replace the project name with @PROJECT_NAME@ in text files only, add
   kind = "game" and the CS_SCREENS markers to their Module.cpp (or a Module.cpp if they have none —
   ForgePong's scripts live in the game template; make each sample self-contained), and verify each
   scaffolds through ScaffoldProjectTo (call it via the existing New Project path with the folder name)
   and opens.
2. templates/game/: replace scenes/Main.cscene with a 2D scene (one sprite entity with a flat colour,
   an orthographic primary camera) authored as JSON; remove any 3D script that AP-05 left, keep the 2D
   sample scripts, register StoryUiBinding in Module.cpp (with its missing include) or delete the file;
   add kind = "game" to project.cproj and the markers to Module.cpp. templates/blank/: manifest with
   kind = "blank", a Main.cscene with canvas + ortho camera only, a Module.cpp with markers only, the
   same CMakeLists.txt. templates/app/: exactly section 8 — manifest with kind = "app" and startup_flow,
   flows/Main.cflow (Home -> Dashboard -> Settings, key:Escape back, @quit from Home), three screens
   whose canvases use UiValueText/UiGauge/UiPlot/UiSlider/UiToggle/UiButton/UiImage/UiHostedPanel with
   the contract's reflected names and fields (author with the RectTransform anchor idioms from
   docs/guide/game-ui.md), src/services/AppService.{h,cpp} (publishes app.uptime, app.sine,
   app.counter each tick; handles counter.increment/counter.reset signals; CS_PANEL("Diagnostics") that
   draws three ImGui::Text lines), src/screens/{Home,Dashboard,Settings}Screen.h from the stub shape,
   Module.cpp with CS_SERVICE(AppService) and the three CS_SCRIPTs between the markers, two small
   placeholder PNGs under assets/ui/ (generate them; no token inside), README.md. Write a unit test that
   loads every template scene through SceneSerializer and asserts the expected components are present
   with the expected reflected names (this is what catches a typo before AP-02's renderer sees it).
3. Projects/PendulumLab/: an external-style project like Projects/AnalysisSample (its own
   CMakeLists.txt refusing in-tree add_subdirectory; add it to the root COSMIC_SKIP_PROJECTS lists next
   to AnalysisSample). Contents per section 8's sample row: PendulumService (RK4 via
   math/Integrators.h IntegrateRK4 at the fixed step; state theta/omega; parameters from bus channels
   settings.length (default 1), settings.gravity (9.80665), settings.damping (0); publishes
   pendulum.angle_deg, pendulum.omega, pendulum.energy, pendulum.period_est (zero-crossing estimate),
   pendulum.running; handles pendulum.start/stop/reset/nudge; CS_PANEL("PhasePlot") drawing an ImPlot
   theta-omega scatter from the bus history), screens Home/Lab/Settings + the Stopped overlay state
   (when energy < 0.01, push; resume_clicked pops), LabScreen.h positioning the bob sprite entity from
   the bus each frame (visuals in the editor, logic in C++), HomeScreen.h animating the title,
   flows/Main.cflow with a channel-guarded when transition and key:Escape, project.cproj kind = "app",
   README.md explaining the demonstration. A self-test host inside the sample (X01SelfTest.cpp pattern,
   env COSMIC_Y02_SELFTEST=<result.json>) that navigates the flow by feeding signals, samples the bus,
   counts PhasePlot draws and writes JSON; AP-Q1 runs it.
4. tests/fixtures/ap04/pendulum_reference.csv (F-PENDULUM, generated by a checked-in script from the
   analytic small-angle solution — Python via "py -3" if present, otherwise PowerShell 5.1; "python"
   is a Store alias on this machine — git add -f), tests/test_pendulumlab.cpp compiling the service
   in-exe (CS_SERVICE registration, ServiceHost + DataBus drive at 1/240): undamped RK4 vs reference
   within 1e-4 rad over 10 s, monotone energy decrease with damping 0.05, period estimate within 1 % of
   2*pi*sqrt(L/g), two runs bit-identical. Extend tests/test_template_scripts.cpp to include every
   template script header and the app template's service (compile smoke + default-field checks).
5. Y01's standalone build: configure PendulumLab from a clean SDK path (-DCOSMIC_SDK_DIR to this
   worktree, fresh build dir outside the tree) and build it; record the exact commands in a wrapper
   tests/acceptance/fixtures/Run-AP04Sample.ps1 and manifests ap04-units / ap04-sample; run them.

Non-vacuity as always (break the RK4 step once; the reference comparison must fail). Both configs
0 warnings; CosmicTests both configs green; both audits exit 0. Evidence in evidence/AP-04/report.md
(WO-10 layout; include the sample-capture provenance and the scene-validation table). Append section-13
rows for templates, samples and PendulumLab as landed. Land per L2: rebase onto main (AP-02 lands
before you), rebuild, retained units, your manifests, audits, commit on ap/04 as kdadabhoy with no AI
trailer (templates, PendulumLab, tests in three commits). Do not push. Return: F02/Y01 results, the
template trees, and any contract deviation (especially widget field names you needed and did not find).
~~~

## Files to read first (and nothing else)

`work-orders/README.md`; `01-Design-Contracts.md` §2, §3, §5, §8; the catalog rows; `docs/guide/game-ui.md`
(anchor idioms) and `docs/guide/flow-and-story.md` (the `.cflow` format); `Projects/AnalysisSample/CMakeLists.txt`
+ `src/X01SelfTest.cpp` (patterns); `Projects/Starforge/assets/templates/game/**` (after AP-01's move).

## Owns / May touch

See §10 (AP-04 row). May touch `tests/CMakeLists.txt` (TUs + fixture define).

## Scope

- **In:** templates, samples, PendulumLab, headless tests, fixture, Y01 build wrapper.
- **Out:** editor code (picker, panels — AP-03), widget rendering (AP-02), docs (AP-D2), running Y02/Y03
  (AP-Q1).

## Deliverables

`templates/{app,game,blank,samples/*}`; `Projects/PendulumLab/**`; `test_pendulumlab.cpp`; the extended
`test_template_scripts.cpp`; F-PENDULUM + generator; wrapper + manifests; `evidence/AP-04/report.md`.

## Done when (DoD)

Every template scaffolds and opens; every template scene loads with the expected components; the
sample builds standalone from a clean SDK path; F02 and Y01 pass; the self-test host exists for Y02.

## Rollback

Revert the lane's three commits.
