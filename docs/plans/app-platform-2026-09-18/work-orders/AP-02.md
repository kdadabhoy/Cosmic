# AP-02 — Bound widgets, hosted-panel collection, interaction, goldens

**Gate:** G2 · **Wave:** 2 (worktree, alongside AP-04 and AP-D1) · **Runs:** worktree `ap/02` ·
**Base:** `main` after AP-P1 has landed · **Depends on:** AP-01 (via main) · **Acceptance:** V03, V04,
V05 (engine half) · **Model:** Fable 5.1 · **Effort:** xhigh · **Status:** not started

The seven new UI components of `01-Design-Contracts.md §3`, rendered only through `Renderer2D`,
interactive in `UiSystem::Update`, reflected so the Inspector/serializer/undo work for free, previewable
without a bus, with one golden per kind. The `UiSystem` signatures already exist (AP-01); this lane
fills them.

## Copy-paste prompt

~~~text
Execute only AP-02 from the Cosmic App Platform packet (docs/plans/app-platform-2026-09-18/). Read
work-orders/README.md (all, lane rules), then ONLY 01-Design-Contracts.md sections 1 (read-side API),
3 and 4 (the collect part), and 03-Acceptance-Catalog.md rows V03, V04, V05 (engine half) and E05.

Lane setup: from C:\dev\Cosmic run  git worktree add ..\Cosmic-ap-02 -b ap/02 main  (main contains
AP-01 and AP-P1), work inside C:\dev\Cosmic-ap-02, set $env:COSMIC_SDK to it, build in its build\.
Read Cosmic/src/scene/ui/UiComponents.h, UiSystem.h and UiSystem.cpp fully, and the UI block of
Cosmic/src/reflect/TypeRegistry.cpp (around lines 200-234) — those are the only engine files you edit,
plus the tests listed in your ownership row. AP-04 and AP-D1 run concurrently in other worktrees; you
never touch templates, docs or Starforge.

Implement section 3 exactly: the seven structs with the given names, fields, defaults and runtime
members; CS_REGISTER_COMPONENT for each; reflection rows in TypeRegistry.cpp with the reflected names
and category "UI", AsAssetPath("texture") on the texture slots, .Color() on colours, .Range where the
contract gives one, enums registered with EnumValue; the pure helpers (FormatValue, GaugeFill,
SliderValueAt) as public statics; CollectHostedPanels; rendering in UiSystem::Render honouring the bus
/ preview rules (bus == nullptr => preview; missing channel => Placeholder in live mode, Preview* in
preview mode; stale colouring from bus->Age); interaction in UiSystem::Update (sliders write every
frame while dragging and emit Signal on release only when changed; toggles flip on release-inside;
the topmost interactable button/slider/toggle wins; Update returns true over any of them; the existing
UiButton behaviour is byte-for-byte unchanged). Plots read bus->History over WindowSeconds, skip
non-finite samples, decimate to at most 512 segments per channel, and never exceed Renderer2D's
MaxLines in one plot. Value text draws through the sibling UiText's font/size/colour/alignment without
modifying its Text field. Every draw goes through Renderer2D (no raw GL, the conformance audit runs).

Tests: tests/test_ui_widgets.cpp (V04 — every bullet, plus a SceneSerializer round-trip of a scene
holding all seven components, field-exact, plus a check that the reflection registry lists the seven
names in category UI). tests/render/render_ap02_widgets.cpp (V03) with F-WIDGETS scenes at 320x180:
generate each golden once, inspect it by eye (describe what you see in the report), commit as
tests/render/goldens/ap02_<kind>.png, compare with the existing tolerance plus sentinel ROIs; also the
E05 A/B pair (preview vs live with identical values => byte-identical; with different values => diff
confined to widget ROIs) — implement it here, AP-03 only re-runs it. Existing goldens must stay
byte-identical (hash before/after). If no GPU is present, the render cases are ENVIRONMENT_BLOCKED and
you say so; do not fake them.

Non-vacuity: break each helper once, watch its test fail, restore, record. Both configs 0 warnings;
CosmicTests both configs green; CosmicRenderTests green (or blocked); both audits exit 0. Manifests
tests/acceptance/manifests/ap02-units.manifest.json and ap02-gpu.manifest.json (+ a wrapper
Run-AP02Render.ps1 modelled on Run-WO08Render.ps1), run through the runner.

Evidence in evidence/AP-02/report.md (WO-10 layout; include the golden descriptions and the sentinel
tables). Append the section-13 rows ("Bound widgets", "Hosted panels (collection)") as landed. Land
per L2: rebase onto main (AP-02 lands first in its wave, so a clean rebase is expected), rebuild
Debug+Release 0-warn, retained units both configs, your manifests, both audits, the link checker if
present, commit on ap/02 as kdadabhoy with no AI trailer (engine + tests in two commits). Do not push.
Return: V03/V04/E05 results, the golden list with descriptions, and any contract deviation.
~~~

## Files to read first (and nothing else)

`work-orders/README.md`; `01-Design-Contracts.md` §1 (reads), §3, §4; `03-Acceptance-Catalog.md`
(V03/V04/V05/E05); `Cosmic/src/scene/ui/{UiComponents.h,UiSystem.h,UiSystem.cpp}`;
`Cosmic/src/reflect/TypeRegistry.cpp:195-240`; `tests/render/render_wo08_primitives.cpp` (as the golden
pattern) and `tests/render/GoldenImage.h`.

## Owns / May touch

See §10 (AP-02 row). May touch `tests/CMakeLists.txt` and `tests/render/CMakeLists.txt` (add TUs).

## Scope

- **In:** components, reflection, rendering, interaction, collection, tests, goldens, manifests.
- **Out:** the host-side ImGui draw of hosted panels (AP-01 player already; AP-03 editor); Inspector
  buttons; docs (AP-D2).

## Deliverables

The component/reflection/UiSystem changes; `test_ui_widgets.cpp`; `render_ap02_widgets.cpp` + goldens;
manifests + wrapper; `evidence/AP-02/report.md`; §13 rows.

## Done when (DoD)

V03/V04 pass (V03 may be ENVIRONMENT_BLOCKED without a GPU, named); E05 A/B implemented and green on
GPU; existing goldens byte-identical; existing UI tests unchanged and green; both audits exit 0.

## Rollback

Revert the lane's two commits.
