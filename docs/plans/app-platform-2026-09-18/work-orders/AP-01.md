# AP-01 — Foundation: DataBus, app services, Data() proxy, flow additions, host wiring

**Gate:** G1 · **Wave:** 1 · **Runs:** `main`, alone, in `C:\dev\Cosmic` · **Base:** the AP-05B commit ·
**Depends on:** AP-05 · **Acceptance:** V01, V02, V06 · **Model:** Fable 5.1 · **Status:** not started

The engine-side glue every later lane builds on: the host-owned `DataBus`, the `AppService` tier with
`ServiceHost`, `CS_SERVICE`, `CS_PANEL` and `PanelRegistry`, the `Data()` script proxy, the flow's
channel guards / `when` transitions / `StartAt` / key bridge, the two hosts wired in the fixed frame
order, the `UiSystem` signatures the widgets lane needs, and the template directory move. Zero behaviour
change for a project that registers no service.

## Copy-paste prompt

~~~text
Execute only AP-01 from the Cosmic App Platform packet (docs/plans/app-platform-2026-09-18/). Read
work-orders/README.md (all), then ONLY 01-Design-Contracts.md sections 1, 2, 4 (host side), 5 (flow
additions, key bridge, manifest key — not the stub), 6 (what must survive), 8 (the game/ move only)
and 14 (anchors), and 03-Acceptance-Catalog.md rows V01, V02, V06. Work alone on main in C:\dev\Cosmic
on top of the AP-05B commit. Revalidate HEAD, status, toolchain and every anchor you touch.

Implement exactly the contract, in this order, building and running the units after each step:
1. Cosmic/src/data/DataBus.{h,cpp} (section 1) with tests/test_databus.cpp (V01, every bullet a
   CHECK; the 100k-Set timing is recorded with MESSAGE, not asserted).
2. Cosmic/src/scripting/AppService.h (AppService, AppContext, PanelRegistry, CS_PANEL),
   scripting/ServiceHost.{h,cpp}, ModuleRegistry AddService/FindService/ServiceNames + UnregisterModule
   stripping services (mirror the script path and the KI-29 ordering), ModuleMacros.h CS_SERVICE, with
   tests/test_servicehost.cpp registering services in-exe (V02 U part). For the V02 W part extend the
   WO-07 lifetime fixture pattern: a fixture DLL that registers a CS_SERVICE and a CS_PANEL, loaded and
   unloaded 20 times through the real GameModule and PlayerLayer paths with a host-owned DataBus whose
   values persist across reloads, asserting OnDetach before FreeLibrary and no callback after unload.
3. ScriptHost::SetDataBus + the Data() proxy on ScriptableEntity and SystemScript (section 2, last
   paragraph), covered in test_servicehost.cpp or a small addition to tests/test_scripthost.cpp.
4. FlowMachine: FlowGuard::Channel, SetDataBus, "when" transitions (once per OnUpdate, after the drain,
   before timers, Validate rejects a when without a guard), StartAt, KeySignals; the lookupChannel
   parameter on EvaluateFlowGuard; JSON load/save byte-stable for v1 files. scene/FlowKeyBridge.{h,cpp}
   with an injectable probe. tests/test_flow_channels.cpp (V06).
5. UiSystem signatures only (section 3 end): the DataBus parameters, the preview flag, the
   UiHostedPanelDraw struct and CollectHostedPanels with a no-op body; every existing call site keeps
   compiling and the existing UI tests stay green unchanged.
6. Host wiring per the section-2 frame-order table. PlayerLayer: m_Bus, m_Panels, m_Services,
   m_KeyBridge; instantiate services after the manifest read and before the flow starts; BindScene
   inside RebindScripts; Advance the bus with the unscaled frame delta; hosted panels drawn in
   OnImGuiRender before the pause check (section 4 block); the Escape edge replaced by the key bridge;
   OnDetach order flow -> scripts -> services -> physics/renderer -> UnregisterModule. StarforgeApp:
   m_PlayBus, m_PlayPanels, m_PlayServices, m_PlayKeyBridge; PlayScene instantiates services
   (InEditor=true) before m_Scripts.Instantiate and binds the scene; StopScene destroys scripts then
   services and leaves the bus intact; ReloadModule stops Play (existing behaviour) — services are
   destroyed by StopScene; TickPlay runs the section-2 order; RenderViewport passes &m_PlayBus while
   playing and nullptr (preview) otherwise; hosted panels are NOT drawn in the editor yet (AP-03).
   Add the hook lines only; put any new logic in a new TU (StarforgeApp.cpp is at the /bigobj limit).
7. Move Projects/Starforge/assets/templates/* into templates/game/ (git mv), point ScaffoldProjectTo
   and the POST_BUILD sync in Projects/Starforge/CMakeLists.txt at it, and read "kind" from
   project.cproj in ProjectManifest only if trivial (else leave to AP-03). Scaffolding a project and
   Ctrl+B must still work — prove it by running the existing C05 lifecycle manifest (wo09-editor).
8. Cosmic.h includes data/DataBus.h, scripting/AppService.h, scripting/ServiceHost.h,
   scene/FlowKeyBridge.h; add their four rows to docs/reference/README.md pointing at
   ../guide/app-authoring.md (AP-D2 writes that chapter; the coverage checker only needs the row).

Every new test must be non-vacuous: for each behaviour, revert the implementation once (stash or a
deliberate break), observe the failure, restore, and record it in the report. Both configs 0 warnings;
CosmicTests both configs green with the new cases counted; both audits exit 0; the retained manifests
wo09-editor and wo09-units pass. Write tests/acceptance/manifests/ap01-units.manifest.json (the three new
TUs filtered by test-suite name) and run it through the runner.

Evidence in evidence/AP-01/report.md (WO-10 layout, plus a "Contract deviations" section — if you had
to deviate from section 1/2/5 signatures, say exactly what and why; AP-Q1 updates the contract).
Append rows to 01-Design-Contracts.md section 13 with status "landed" for what you built. Commit
locally as kdadabhoy, no AI trailer, in up to three commits (engine, hosts+templates, tests+docs). Do
not push. Return: the V01/V02/V06 results both configs, the deviations list, the reload proof, and the
new test count.
~~~

## Files to read first (and nothing else)

`work-orders/README.md`; `01-Design-Contracts.md` §1, §2, §4, §5, §6, §8, §14; `03-Acceptance-Catalog.md`
(V01/V02/V06); `Cosmic/src/scripting/{ModuleRegistry.h,ModuleMacros.h,ScriptHost.h,ScriptableEntity.h}`;
`Cosmic/src/scene/FlowMachine.h`; `Cosmic/src/layers/PlayerLayer.{h,cpp}`; the `PlayScene/StopScene/
TickPlay/RenderViewport/ReloadModule` functions of `Projects/Starforge/src/StarforgeApp.cpp` (by anchor).

## Owns / May touch

See `01-Design-Contracts.md §10` (AP-01 row). May touch `tests/CMakeLists.txt` (add TUs) and
`docs/reference/README.md` (four rows).

## Scope

- **In:** everything in the prompt.
- **Out:** widget rendering/interaction (AP-02); editor panels, gizmo, source links, live-loop UX
  (AP-03); template contents (AP-04); docs beyond the four manifest rows (AP-D2).

## Deliverables

The headers/TUs named in the prompt; three test TUs; `ap01-units` manifest; `evidence/AP-01/report.md`;
the §13 rows.

## Done when (DoD)

V01/V02/V06 pass Debug+Release; both hosts follow the frame-order table; `StopScene` and `ReloadModule`
destroy services and leave `m_PlayBus` intact; the template move keeps scaffold + Ctrl+B working
(wo09-editor green); zero behaviour change without services (existing suites unchanged); both audits
exit 0.

## Rollback

Revert the up-to-three commits in reverse order.
