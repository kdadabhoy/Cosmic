# WO-07 — Runtime/plugin/module/UI teardown (and fix KI-1)

**Gate:** G3 · **Depends on:** WO-04; coordinate WO-05/06 · **Acceptance:** L01–L05, P01 · **Status:** not started

Prove the two DLL lifecycles tear down cleanly, fix the confirmed snap-chip abort (KI-1), and make
UI tests catch ImGui stack imbalance in Release (where it is silent).

## Copy-paste prompt

~~~text
Execute only WO-07 from the Cosmic 2D stability packet. Read work-orders/README.md (KI-1), ../02
(WO-07) and ../03 (L01-L05, P01, F-LIFETIME). Work in codex/2d-stability, COSMIC_2D_ONLY=ON, via the
WO-04 runner on a real GPU (hidden window OK).

Two SEPARATE lifecycles: the Application runtime plugin (Application.cpp adopts ImGui/ImPlot
contexts at :716-717 before CreatePluginLayer :703) and the Starforge game module (GameModule.cpp,
its own unload path; ReloadModule preserves a serialized edit scene, clears selection/undo, stops
Play — it does NOT promise arbitrary running C++ state). Test them independently. Running
Starforge.exe --project only exercises the runtime host override, not the editor module lifecycle.

FIX KI-1: Projects/Starforge/src/ViewportController.cpp:1284-1293 — the snapChip lambda pushes a
style colour guarded on `on`, the button flips `on`, and the pop is guarded on `on` AGAIN, leaving
ImGui's style stack unbalanced on every click (Debug abort / Release corruption). Apply the same fix
the toggle lambda already uses (`const bool pushed = on;`, :1322-1331). Add a failing-before test
and a passing-after result. These chips are outside the COSMIC_2D_ONLY fence, so they ship in the 2D
editor.

Cover: OnAttach/OnDetach/destruction order; module-owned components/scripts, Log sinks,
EntitySelection/EventBus listeners, jobs, file watchers, audio, hotkey callbacks, project fonts,
textures and ImGui/ImPlot contexts. Runtime reload: define reset/reopen. Editor reload: preserve
serialized scene, document cleared selection/undo + stopped Play. Exercise repeated open/close,
modal dialogs, cancelled browsing, failed plugin/module loads, failed builds, close during
background activity, return-to-launcher. Any module UI usage must have a valid adopted context.

L05 must assert ImGui style/ID stack balance after EACH scripted UI action (Release-safe: read the
stack sizes), not merely "no crash" — KI-1 proves a whole-frame no-crash check misses it.

Commit locally per D-commit. Do not push. Return: KI-1 fix + regression, L01-L05/P01 results,
balanced-resource evidence after warmup, and proof stale callbacks cannot run after unload.
~~~

## Scope
- **In:** runtime-plugin + editor-module teardown coverage; KI-1 fix; the stack-balance assertion;
  50 real editor rebuild/reloads + repeated host loads; P01 ImPlot context coverage.
- **Out:** serial/recording internals (WO-05/06); renderer primitive coverage (WO-08).

## Deliverables
KI-1 fix in `ViewportController.cpp`; `tests/` for L01–L05/P01; the stack-balance assertion helper.

## Done when (DoD)
L01–L05 and P01; KI-1 fixed with failing-before/passing-after; 50 editor rebuild/reloads + repeated
host loads clean; balanced owned resources after quiescence; stale callbacks cannot execute after
unload; UI tests fail on any stack imbalance.

## Rollback
Revert the `ViewportController.cpp` fix + tests. KI-1 stays open in `known-issues.md` until re-fixed.
