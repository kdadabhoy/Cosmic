# WO-12 — Documentation cleanup after separation

**Gate:** G4 · **Depends on:** WO-03–WO-11 · **Acceptance:** DOC01–DOC03 · **Status:** not started

You explicitly asked for this cleanup. One authoritative 2D support/build/release policy; preserved
but clearly-parked 3D history; obsolete prompts marked historical so a future AI won't run them.

## Copy-paste prompt

~~~text
Execute only WO-12 from the Cosmic 2D stability packet. Read work-orders/README.md, ../02 (WO-12),
../03 (DOC01-DOC03). Work on main (main-only campaign). Inventory README.md, docs/guide, docs/reference,
docs/systems, docs/design, docs/engineering-notes, docs/plans and SF_Telem docs.

Make ONE support/build/release policy authoritative:
  - Update all main / engine-2d / engine-3d descriptions, fresh-checkout commands, presets,
    packaging, test profiles and known limits to the accepted 2D-only trunk with engine-3d as the
    parked full-tree snapshot.
  - Write the D-CPU SSE4.2 floor and the D-9km deferral into the support/consumer docs.
  - REMOVE the active requirement to keep branches byte-identical or to test parked 3D. Keep those
    notes only as dated history.

Preserve, don't destroy, 3D technical/reference history — mark it parked and outside the supported
trunk; archive superseded plans with origin/date/commit + replacement links. Do NOT mass-delete
useful explanations or break docs/reference/README.md's public-header coverage manifest (the
tests/check_docs_coverage.ps1 gate). Keep old command prompts clearly HISTORICAL.

Reconcile stale content against source: README internal clock examples vs the current float
accumulator (Application.cpp:215-218); "every test passed" language; the "6 goldens vs 6 cases"
wording (render_2d.cpp is 5 goldens + 1 A/B); SF screen documentation; and the new lifecycle/data/
serial-close/CSV-grammar contracts from WO-05/06. Label skeletons honestly. Finish only the
2D-facing docs needed for the qualified workflows — a full 3D-doc rewrite is out of scope.

Commit locally per D-commit. Do not push. Return: DOC01-DOC03 results and confirmation a new AI can
build/test/package a 2D app from the current documented path alone.
~~~

## Scope
- **In:** one authoritative 2D policy; branch descriptions; parked-3D marking; stale-content
  reconciliation; coverage/link checks; the D-CPU/D-9km write-ups.
- **Out:** rewriting all 3D documentation; new feature docs (A1–A5).

## Deliverables
Updated README + `docs/` policy/reference; archived-plan pointers; DOC01–DOC03 evidence.

## Done when (DoD)
DOC01–DOC03; link/coverage checks pass; one consistent main/engine-3d policy; obsolete dual-test/
byte-identical instructions are clearly historical; a new AI can build/test/package a 2D app using
only the current documented path.

## Rollback
Docs-only — revert the commit.
