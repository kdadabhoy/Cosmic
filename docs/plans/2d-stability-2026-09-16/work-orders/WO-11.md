# WO-11 — Package and install the same qualified 2D app

**Gate:** G4 · **Depends on:** WO-03, WO-05–WO-10 · **Acceptance:** K01–K04 · **Status:** not started

One consistent 2D build identity from source to clean-machine install, with writable user-data
paths. Unify the divergent layouts (`cmake --install` puts `CosmicApp.exe` at root; `release.yml`
renames it to `$app.exe`).

## Copy-paste prompt

~~~text
Execute only WO-11 from the Cosmic 2D stability packet. Read work-orders/README.md, ../02 (WO-11),
../03 (K01-K04). Work on main (main-only campaign), COSMIC_2D_ONLY=ON.

Reconcile the packaging paths: cmake --install (Runtime/CMakeLists.txt:74 installs CosmicApp as-is),
the package*.bat scripts, Starforge's Package UI, and release.yml staging (:39 renames
CosmicApp.exe -> $app.exe, hand-copies assets, writes boot.cfg). Unify or prove equivalent behaviour;
enforce the 2D build identity (WO-03) through ALL of them; preserve only the selected app's payload
plus required runtime/assets/licenses (no excluded 3D app or dev-only test target). No 3D or mixed
binary may ship.

Build an external consumer from a CLEAN SDK path (no source-relative hidden dependency; PUBLIC 2D
mode + DLL/CRT identity agree). Package SF_Telem and the WO-10 analysis sample.

Writable paths: SF_Telem redirects logs to project://logs (SF_Telem.cpp:62) and records to relative
recordings/ — these must resolve to a WRITABLE user-data location, never the read-only install dir.
Test recording/replay/record/export/config/screenshot from a read-only install location as a normal
user; opening the main window is NOT sufficient.

On Windows 10 AND 11: portable + installed launch, arbitrary CWD, spaces/Unicode paths, missing
assets, no SDK/compiler on target, VC++ runtime availability. A VM without suitable graphics cannot
certify OpenGL rendering — mark that ENVIRONMENT_BLOCKED, not passed.

Commit locally per D-commit. Do not push, publish, or install onto Kaden's production SF setup.
Return: K01-K04 results, the unified layout, and the writable-path proof.
~~~

## Scope
- **In:** unify/verify packaging layouts under one 2D identity; external-consumer build; SF_Telem +
  analysis-sample packages; clean Win10/Win11 install; writable user-data paths.
- **Out:** installer feature changes beyond 2D identity + user-data policy; publishing a release.

## Deliverables
Reconciled install/package/staging config; `K01–K04` evidence; the writable-user-data policy in
`contracts/`.

## Done when (DoD)
K01–K04; staged artifacts and installer results match the candidate build identity; no source-tree
or developer-PATH dependency; user-data writes succeed from a read-only install; GPU-less VM steps
are marked blocked, not passed.

## Rollback
Revert packaging/config commit; the candidate returns to WO-03 packaging.
