# WO-10 — Clocks, numerics and a synthetic analysis sample

**Gate:** G3→G4 · **Depends on:** WO-06, WO-08 · **Acceptance:** N01–N04, X01 · **Status:** not started

Pin the real Application clock/dispatch semantics (not just the standalone widget), the numeric
toolkit, and a synthetic analysis specimen. **to-9km is deferred (D-9km):** the sample uses
synthetic fixtures only and is a compatibility specimen, not consumer qualification.

## Copy-paste prompt

~~~text
Execute only WO-10 from the Cosmic 2D stability packet. Read work-orders/README.md (D-9km), ../02
(WO-10), ../03 (N01-N04, X01, F-TRAJECTORY/F-SERIES-LARGE). Work in codex/2d-stability,
COSMIC_2D_ONLY=ON, via the WO-04 runner.

Clocks: pin the ACTUAL Application fixed/variable dispatch, not only TimelineState. Note the current
float clock: m_Accumulator += frameTime*m_TimeScale, signed dt on rewind (Application.cpp:215-218) —
negative TimeScale accumulates, it is NOT reverse physics. Cover clock injection (or an isolated
production seam), pause/resume, speeds 0/0.25/1/4, >0.25 s stall, long uptime, render-rate variation,
distinguishing global vs plugin-local scaling. Define safe handling of unsupported negative GLOBAL
physics scale (reject or documented safe policy, no hidden restart debt). N01 MUST drive
Application/host dispatch, not solely TimelineState.Advance/FixedSubstepper.

Numerics: reuse RK4 projectile (dt=1/480, 1e-4 rel), oscillator (2,000 steps @1/1000, 2e-3), RK4
halving-ratio 10..24, semi-implicit energy <1.10x over the 10 s case; canonical PCG integer vector +
1,000,000 outputs same seed/stream, exact same-build reproducibility. Keep scientific values as
DOUBLE where a consumer needs them; never require cross-GPU image or MP4 bit equality.

Analysis sample (compatibility specimen only — D-9km): build a small project OUTSIDE the SDK checkout
using existing primitives/ImPlot/CSV/replay/PNG. Load F-TRAJECTORY (x=30t, y=50t-0.5*9.80665*t^2,
t=i/120 for i=0..1200 => 1,201 samples, double reference) + F-SERIES-LARGE; animate a marker/trail,
plot position/speed, play/pause/scrub at exact/midpoint times, export a correct PNG, then package and
run. Test a 1e11 origin offset subtracted in DOUBLE before float display (this tests the sample's
conversion, not a new engine-wide large-coordinate promise). Do NOT migrate the engine to double. Do
NOT build A5's reusable widget library here.

Commit locally per D-commit. Do not push. Return: N01-N04/X01 results, the negative-global-scale
policy, and an explicit note that real to-9km qualification remains deferred.
~~~

## Scope
- **In:** Application dispatch/clock coverage; numeric toolkit tests; a synthetic analysis sample
  built outside the SDK; the 1e11 double-before-float conversion case.
- **Out:** real to-9km schema/qualification (D-9km); engine-wide double migration; A5 widgets;
  animated export (A1 backlog).

## Deliverables
`tests/` for N01–N04; the analysis sample project (outside the SDK tree) + X01 evidence; the
negative-global-scale policy in `contracts/`.

## Done when (DoD)
N01–N04 and X01; the sample builds outside the SDK checkout, plots/animates/scrubs by the defined
time base and exports a correct still; the wall-clock scheduler is proven (not only the unit).
Explicitly records that consumer (to-9km) qualification is deferred.

## Rollback
Additive tests + a standalone sample — revert the commit; no engine behaviour change (clock seam is
inert unless tested).
