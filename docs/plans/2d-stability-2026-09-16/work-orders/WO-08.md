# WO-08 — Renderer, camera and capture safety net

**Gate:** G3 · **Depends on:** WO-03, WO-04 · **Acceptance:** R01–R07 · **Status:** not started

Extend the existing golden harness (do not replace its comparator) to cover every primitive, batch
boundary, real 2D instancing, camera/RTT edges and PNG capture.

## Copy-paste prompt

~~~text
Execute only WO-08 from the Cosmic 2D stability packet. Read work-orders/README.md, ../02 (WO-08),
../03 (R01-R07, F-2D) and reuse docs/plans/29-phase30-2d-hardening-plan.md P1-P3/P7 as case-design
input. Work on main (main-only campaign), COSMIC_2D_ONLY=ON, via the WO-04 runner on a real GPU.

Baseline facts to record and respect:
  - Current golden resolution is 320x180 (tests/render/GoldenImage.h:56-57); channel tolerance 2/255,
    pixel budget 0.1% (:49,:52). State this baseline in the report.
  - render_2d.cpp has 6 cases: 5 goldens (sprites/tilemap/light2d/ui/scene2d) + 1 in-process A/B
    (light2d). There is NO 2D instancing golden — instancing.png belongs to render_3d.cpp:447-511.

Do:
  - Test every primitive/text/font path; batch limit +/-1 (limit 10,000); texture slot rollover
    (30/31/32/33 non-white textures, slot 0 reserved); mixed draw/state transitions. Assert actual
    flush/draw counts for isolated homogeneous cases with sentinels for missing/duplicated items.
  - ADD real 2D instancing coverage: DrawInstancedCircles/DrawInstancedQuads at 0,1,19999,20000,
    20001,40001; expect ceil(N/20000) instance draws (10,000 => 1). Add a NEW 2D instancing golden —
    do NOT reuse the 3D instancing golden.
  - Camera: pan/zoom anchors, viewport offset, 100/125/150/200% DPI, default zoom half-height
    0.01..10,000, zero/negative/nonfinite input policy, an odd/non-standard target size (e.g.
    641x359 — justify the choice as a deliberately awkward non-multiple-of-8 target) and a changing
    viewport. Round-trip <=0.5 px in the declared local range; no NaN/divide-by-zero/resize crash.
  - RTT: nested/multiple RenderPass cameras, two RTT targets into a UiImage; verify camera/viewport/
    FBO/blend/depth restoration + no source-target feedback; 200x resize/create/destroy leaks no GPU
    object. PNG: CPU encode/decode exact 4-corner fixture, GPU readback top-left RGBA8 orientation
    + defined HDR-to-byte.

Measure 10,000-instance performance ONLY on a named machine (R07); keep correctness separate.
Original goldens change only for a separately justified intentional correction — never to bless a
regression. Commit locally per D-commit. Do not push. Return: R01-R07 results, the new 2D instancing
golden's provenance, and any correctness/perf split.
~~~

## Scope
- **In:** primitive/batch/instancing/text coverage incl. a new 2D instancing golden; camera/DPI/RTT
  edges; PNG capture; named-machine perf.
- **Out:** renderer ordering/architecture redesign; 3D render paths; new visual features (A3/A4).

## Deliverables
`tests/render/` additions + one new 2D instancing golden; R01–R07 evidence; the golden-baseline
note (320×180) in the report.

## Done when (DoD)
R01–R07; reviewed images and counter assertions agree; a real 2D instancing golden exists (not the
3D one); existing goldens unchanged except for a documented intentional correction.

## Rollback
Additive tests + new golden — revert the commit; existing goldens untouched.
