# WO-04 — Make acceptance executable, plus the injectable serial transport seam

**Gate:** G2 · **Depends on:** WO-02 (uses WO-05a's seam spec) · **Acceptance:** H01–H04 · **Status:** **DONE 2026-09-17** (local commit, not pushed). Seam + acceptance runner built; H01–H04 self-tests pass; seam smoke green both configs; seam proven inert in the shipping build. Evidence: `../evidence/WO-04/` (`WO-04-report.md`, `seam-and-inertness.txt`, `honest-gates.txt`, `acceptance-selftest/`). KI-2 seam ✅ (KI register updated); KI-4 left unfixed for WO-05.

The runner that turns the catalog into executable, observable evidence — **and** the injectable
transport seam without which the connected-state serial tests (WO-05) cannot run headlessly (KI-2).
The review pulled the seam out of WO-05 into here as a first-class deliverable.

## Copy-paste prompt

~~~text
Execute only WO-04 from the Cosmic 2D stability packet. Read work-orders/README.md (global rules +
KI-2 and KI-4), ../02-Stability-Work-Orders.md (WO-04), ../03-Acceptance-Test-Catalog.md (execution
model, evidence contract, H01-H04), and — this is the spec you implement — WO-05a's three evidence
files: evidence/WO-05a/transport-seam-spec.md, hazard-analysis.md, test-run.md. Work on main
(main-only campaign, D-WORKFLOW), COSMIC_2D_ONLY=ON.

Two deliverables.

(1) Injectable serial transport seam — build EXACTLY what evidence/WO-05a/transport-seam-spec.md
specifies. Inject ONLY at the four OS-boundary calls SerialPort makes (CreateFileA/DCB/timeouts;
overlapped ReadFile + WaitForMultipleObjects; WriteFile; CancelIoEx/CloseHandle) plus the registry
List(). Leave the ENTIRE state machine real and untouched: both std::thread(s), m_Abandon, the stop
event, every State transition, and SerialLink's auto-reconnect policy. No raw-handle exposure, no
private-flag mutation, no second simulated app, no parser reimplementation.
  - Add ISerialTransport (Open/Read/Write/Close/List), a Win32SerialTransport that is today's code
    verbatim and the DEFAULT, and a FakeSerialTransport with the test controls the spec lists:
    SetOpenResult, SetOpenBlockMs (blocks inside Open, honoring the stop handle — this is the KI-4
    exit-hang lever), PushBytes, SignalDrop, SetAvailablePorts, plus OpenCount/CloseCount/Written
    observers.
  - Inject via a test-only ctor on SerialPort (default = Win32) and a matching pass-through ctor on
    SerialLink, so its reconnect BeginOpen path can be driven without hardware.
  - Prove inertness: the shipping build is byte-identical behavior. Re-run tests/test_serial_lifecycle.cpp
    green against the Win32 default BEFORE wiring any fake, and assert the seam is unreachable/off in
    the distributed build. Nothing test-only ships in a package.
  - Seam smoke test (WO-04's own coverage; the full matrix is WO-05): with FakeSerialTransport, drive
    the REAL SerialLink/SerialPort commands to (a) SetAvailablePorts + Connect + PushBytes -> assert
    IsReceiving(), ConsumeJustConnected() one-shot, Poll() returns the exact bytes; (b) SignalDrop ->
    State::Failed -> auto-reconnect re-opens; (c) SetOpenBlockMs(large) + Connect + teardown ->
    demonstrate the KI-4 join behavior is now observable under test. Do NOT fix KI-4 here (that is
    WO-05, with failing-before/passing-after) — WO-04 only makes it testable.

(2) Minimal acceptance runner — the review pulled the seam into WO-04 alongside this. Build
tests/acceptance/Run-Acceptance.ps1 + a small runner library + manifest/fixture metadata:
  - process runner with PER-RUN temp + user-data dirs; independent per-case deadlines (timeout =
    FAILED, kills ONLY its own child, parent survives, diagnostics preserved); explicit mode/test-count
    checks; JSON + JUnit evidence capturing exit code, logs, hang/crash evidence, metrics,
    actual/expected/diff images, environment (OS build, CPU features, RAM, GPU/driver/GL), seed,
    fixture hash. All paths parameterized — no absolute user paths.
  - tier separation: pure deterministic (U), Windows integration (W), hidden-window GPU (G), real
    editor UI (I), qualified/soak (Q). At startup a missing capability => ENVIRONMENT_BLOCKED, never a
    passing empty suite; a filtered suite with zero expected tests is a FAILURE.
  - Prove it with the H01-H04 self-tests: intentional nonzero exit + crashing fixture => failure with
    logs/dump; a hanging child => timeout + child-only kill + parent survives; missing
    test/fixture/golden => failure AND update-goldens cannot be enabled in acceptance; missing
    GPU/COM/Windows => environment-blocked with no phantom pass; then a normal minimal case passes.

State since the packet was authored (revalidate first, don't assume):
- Live remote origin/main is now 490209e (Kaden pushed WO-03 + the 3D-project deletion). HEAD on main
  is 6b48f4c (WO-05a, local + UNPUSHED, main 1 ahead). WO-00/01/02/03/05a are DONE.
- KI-3 resolved (WO-03). KI-4 opened (WO-05a): the reported COM close/link-loss crash = an exit
  soft-hang — teardown's SerialPort::Close() joins a reconnect worker blocked in CreateFileA and
  m_Abandon does not cancel the in-flight open. OnDetach IS reached on close (via
  WorkspaceLayer::ClearViewportLayer, not the LayerStack). Your seam must make this reproducible under
  test; the fix is WO-05.
- STANDING: no COM/serial hardware, no virtual COM, ever. The seam is the ONLY headless path — every
  W-tier serial case is seam-driven. The reference machine's only registry port is a legacy COM1 UART;
  never Connect() to it in a test (it opens real hardware).
- WO-05a left a diagnosis seat at tests/test_serial_shutdown_race.cpp: a reachable COM999 teardown
  guard plus two doctest::skip() ENVIRONMENT_BLOCKED cases (H1 exit hang, H3 drop). Your seam is what
  lets WO-05 un-skip those; you may add a co-located seam smoke test but leave the skipped seats for
  WO-05.
- The ratified serial-close contract (WO-00 open-Q resolution): bounded cancellation-based close,
  stalled-open signals cancel and returns within the 2 s budget, reconnect intent cleared before
  teardown, <=1 worker. WO-04 does not implement the fix, but design the seam so WO-05 can assert
  exactly this.

Locked decisions — do not reopen, record evidence only: D-CPU (SSE4.2 floor), D-9km (deferred),
D-GPU (target only), D-WORKFLOW (main-only, NO candidate branch/worktree; never touch engine-3d or
the cosmic-pre-2d-2026-09-16 tag), D-commit (author=committer kdadabhoy, NO Co-Authored-By/AI/
"Generated with" trailer, Claude NEVER pushes). Configure/build with -DCOSMIC_2D_ONLY=ON; both Debug
and Release must stay 0-warning and the full unit suite green. Preserve the untracked root file
"Cosmic - 2D Trunk Consolidation & Acceptance Plan.md" — never stage, move, or overwrite it.

Honest gates: no golden regeneration, no tolerance loosening, no retry-until-green; a missing
capability is ENVIRONMENT_BLOCKED with the prerequisite named, not a pass. Produce evidence keyed by
commit SHA, dirty-diff, mode/config, environment, exact command + exit code, seed, fixture hash under
evidence/WO-04/. Append any new crash/hang/data-loss finding to the KI register in work-orders/README.md.

Commit locally per D-commit (author kdadabhoy, no AI trailer). Do not push. Return: the runner + seam
design, proof the seam is inert in the shipping build, H01-H04 evidence, the seam smoke-test results
(both configs), and the list of acceptance/serial IDs now executable vs still environment-blocked.
~~~

## Scope
- **In:** the acceptance runner + evidence format + fixtures scaffolding; tier/profile separation;
  the injectable serial transport seam; H01–H04 self-tests.
- **Out:** authoring the individual T/D/R/C/N cases (their own WOs); real hardware qualification.

## Deliverables
`tests/acceptance/Run-Acceptance.ps1` + runner library + fixture metadata; the transport seam in
`Cosmic/src/serial/`; H01–H04 evidence.

## Done when (DoD)
H01–H04 prove the runner catches intentional failure, hang, missing suite/golden and missing GPU,
then a normal minimal case succeeds. The seam lets a fake transport drive the real connection state
machine. Nothing test-only ships in a package.

## Rollback
Runner + seam are additive; revert the commit to remove them. No behaviour change to production
serial paths (the seam is inert unless a test drives it — assert this).
