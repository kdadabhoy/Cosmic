# WO-09 — Authored 2D content and shared services

**Gate:** G3 · **Depends on:** WO-03, WO-04 · **Acceptance:** C01–C06 · **Status:** not started

Boundary/lifetime coverage for the shared 2D surface the brief omitted: sprites, tilemaps, UI,
flow/story/events, scene/prefab/material JSON, and shared physics/assets/audio/scripts/jobs.

## Copy-paste prompt

~~~text
Execute only WO-09 from the Cosmic 2D stability packet. Read work-orders/README.md, ../02 (WO-09),
../03 (C01-C06, F-CONTENT/F-CORRUPT) and reuse docs/plans/29-phase30-2d-hardening-plan.md P2-P6.
Work in codex/2d-stability, COSMIC_2D_ONLY=ON, via the WO-04 runner.

Cover with seeded fuzz + fixed regression fixtures + a parser deadline:
  - Sprites: 0/1/10,000+ items, equal sort keys, flips, active/disabled hierarchy, zero/negative/
    nonfinite transforms; animation loop/end/large-delta. Deterministic order; no invalid draw data.
  - Tilemaps: 0/1/1024 cells/dim, 1025 invalid, max 1,048,576 cells; culling at edges/extreme zoom;
    atlas indices; batch rollover. No overflow/out-of-range; rendered/cull counts match an
    independent visible-cell calc.
  - UI: layout/hit-test, inverted rects, deep hierarchy + cycles, 1,000+ controls, canvas-scale
    extremes; multiple 2D lights (zero/negative radius, offscreen, odd buffer dims). Bounded
    traversal; ratify depth/light ceilings against WO-00.
  - Flow/Story/EventBus: cycles, dangling targets, missing entry, malformed JSON, listener add/remove
    during emit, nested emit, clear. Bounded, deterministic, correct listener counts/lifetimes.
  - Scene/prefab/material/config: valid round-trip + seeded malformed input; UNKNOWN/3D component
    blocks preserved on 2D load/save (this is a 2D compatibility obligation); create/import/save/
    reopen/play/stop/undo/redo/delete on a real project with no user-data loss.
  - Shared services: existing 2D physics/Jolt tests; asset/VFS/cache failure+unload; audio
    lifecycle; scripts; jobs; EnTT storage BELOW/AT/ABOVE its page size. Owners shut down cleanly;
    no out-of-page contiguous access, callback leak, or callback into an unloaded owner.

Apply targeted fixes with regressions — not a new ECS or scene architecture. Commit locally per
D-commit. Do not push. Return: C01-C06 results and each retained service's boundary/lifetime
coverage.
~~~

## Scope
- **In:** sprite/tilemap/UI/light/flow/story/event coverage; scene/prefab/material JSON round-trip +
  unknown-block preservation; shared physics/asset/audio/script/job lifetime + EnTT page boundary.
- **Out:** new content features; ECS redesign; 3D content.

## Deliverables
`tests/` additions for C01–C06; any targeted fixes + regressions; unknown-block preservation test.

## Done when (DoD)
C01–C06 and the existing 2D tests pass; each retained service has meaningful boundary/lifetime
coverage; unknown/3D-authored component data survives a 2D load/save round-trip.

## Rollback
Additive tests + targeted fixes — revert the commit.
