# WO-01 — Preserve 3D and reference provenance

**Gate:** G1 · **Depends on:** WO-00 + migration authorization · **Acceptance:** B01 · **Status:** DONE (2026-09-17)

Preserve the full current tree before the supported trunk changes. Proves preservation, not 3D
runtime stability.

> **DONE (2026-09-17).** The rollback point is durable and the campaign is **main-only**:
> - `engine-3d` was created at `0e8894b8540029ac57e68540aa9774cf5cf77ebe` and **pushed to origin**
>   (verified live: `origin/engine-3d` == `0e8894b`; `engine-3d^{tree}` == `main^{tree}` ==
>   `615bca87964b49b8b03092aeb36276f91ce5cb24`).
> - Annotated provenance tag **`cosmic-pre-2d-2026-09-16`** created on `0e8894b` (tag object
>   `76ca58b…`, peels to `0e8894b`, tagger `kdadabhoy`). **Kaden pushes it:**
>   `git push origin refs/tags/cosmic-pre-2d-2026-09-16`.
> - Every pre-existing branch preserved on origin: `SF-Stable` (`fa6ed9f`), `engine-2d` (`0e8894b`),
>   `feature/engine-split` (`cccab7a`), `phase-7-3d-foundations` (`1bf1dc6`).
> - Empty cherry-pick list — main already contains every donor commit.

## Workflow decision (Kaden, 2026-09-17): MAIN-ONLY

**All WO work happens directly on `main`**, committed locally, and Kaden pushes. There is **no
`codex/2d-stability` candidate branch and no second worktree** — Kaden rejected them as unneeded
ceremony for a solo project. The sole rollback point is `engine-3d` (+ the `cosmic-pre-2d-2026-09-16`
tag). This **supersedes** the candidate-worktree/ff-only-merge model in `../04-Migration-Runbook.md`
§3/§7 and the earlier draft of this WO. A future session must **not** reintroduce a candidate
branch/worktree. Accepted tradeoff: `main` is briefly half-migrated during WO-03..WO-12; nothing else
consumes it.

## What is left for Kaden

One command, when convenient (the AI never pushes, per D-commit):

~~~powershell
git push origin refs/tags/cosmic-pre-2d-2026-09-16
git ls-remote origin refs/tags/cosmic-pre-2d-2026-09-16 'refs/tags/cosmic-pre-2d-2026-09-16^{}'
~~~

The tag object SHA (`76ca58b…`) differs from the commit; the peeled `^{}` must resolve to
`0e8894b8540029ac57e68540aa9774cf5cf77ebe`. (Pushing the tag is optional insurance — `engine-3d` is
already the durable rollback point; the tag is a second immutable pin in case the branch is ever
moved.)

## Scope
- **In:** verify engine-3d identity (done); create the provenance tag (done, local); confirm every
  branch preserved (done); record the rollback point. Main-only — no worktree.
- **Out:** any change to engine-3d; any push (Kaden's); any 3D build/test.

## Deliverables
- `engine-3d @ 0e8894b` pushed (done) + `cosmic-pre-2d-2026-09-16` tag (created; Kaden pushes).
- The rollback reference recorded: `engine-3d`/`engine-2d` both at `0e8894b`; the pre-2D tree is
  `615bca8…`.

## Done when (DoD)
B01 satisfied: a documented, durable rollback/reference point exists **before** supported defaults
change; every existing branch is preserved. **Met** — engine-3d is pushed and the tag is created.
No candidate worktree is created (main-only, by decision).

## Rollback
`git branch -d engine-3d` / `git tag -d cosmic-pre-2d-2026-09-16` only if explicitly re-authorized —
they are the preservation refs and are normally never removed.
