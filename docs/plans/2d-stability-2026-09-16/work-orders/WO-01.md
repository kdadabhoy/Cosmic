# WO-01 — Preserve 3D and reference provenance

**Gate:** G1 · **Depends on:** WO-00 + migration authorization · **Acceptance:** B01 · **Status:** partially done

Preserve the full current tree before the supported trunk changes, and stand up the isolated
candidate worktree. Proves preservation, not 3D runtime stability.

> **Already done this session (2026-09-16):** the local branch `engine-3d` was created at
> `0e8894b8540029ac57e68540aa9774cf5cf77ebe`. Verified: `engine-3d` commit == snapshot, and
> `engine-3d^{tree}` == `main^{tree}` == `615bca87964b49b8b03092aeb36276f91ce5cb24`. It is
> **local-only** — remaining WO-01 work is durability (push + tag, run by Kaden) and the candidate
> worktree.

## Copy-paste prompt

~~~text
Execute only WO-01 from the Cosmic 2D stability packet. Read work-orders/README.md and
../04-Migration-Runbook.md. Revalidate: `git status --short`, `git branch -a`, `git worktree list`,
`git rev-parse main`, and (read-only) `git ls-remote --heads origin`. Confirm main is still
0e8894b8540029ac57e68540aa9774cf5cf77ebe and that engine-3d already points there with a matching
tree; if either differs, STOP and report.

Do NOT edit or build engine-3d. Do NOT push and do NOT promote main — Kaden runs every push.

1. Verify engine-3d identity (commit, tree, and that every pre-existing branch still resolves).
2. Prepare the durable-preservation commands for Kaden to run (do not run them): push engine-3d,
   and create + push the annotated provenance tag cosmic-pre-2d-2026-09-16 on 0e8894b. Write them
   into the WO-01 evidence note verbatim, and verify the peeled ^{} commit expectation in text.
3. Create ONLY the authorized isolated candidate worktree with `git worktree add -b
   codex/2d-stability C:\dev\Cosmic-2D-Stability 0e8894b...`. Never assume C:\dev\Cosmic-2D or
   C:\dev\Cosmic-2D-Stability already exists — inspect `git worktree list` first. Optionally add
   the detached baseline worktree the runbook §3 lists.
4. There is no cherry-pick: the port list is empty (main already contains every donor commit).

Commit any evidence note locally per D-commit. Return: verified SHAs/trees, the exact push+tag
commands staged for Kaden, the worktree path created, and the documented rollback/reference point.
~~~

## Scope
- **In:** verify engine-3d; stage durability commands for Kaden; create the `codex/2d-stability`
  worktree; record the rollback point.
- **Out:** any change to engine-3d; any push/tag execution (Kaden's); any 3D build/test.

## Deliverables
- `codex/2d-stability` worktree at `C:\dev\Cosmic-2D-Stability`.
- WO-01 evidence note: verified commit/tree SHAs, the staged push + `git tag -a
  cosmic-pre-2d-2026-09-16` commands, and the rollback reference (`main`/`engine-3d` both at
  `0e8894b`).

## Done when (DoD)
B01 evidence exists; a documented rollback/reference point exists **before** supported defaults
change; every existing branch is preserved; the candidate worktree is ready. Durability push/tag
are staged for Kaden (not executed by the AI).

## Rollback
`git worktree remove` the candidate if unused; `git branch -d engine-3d` only if explicitly
re-authorized (it is a preservation branch — normally never removed).
