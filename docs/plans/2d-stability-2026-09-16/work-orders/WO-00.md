# WO-00 — Ratify contracts and pin the retained surface

**Gate:** G0 · **Depends on:** this planning review (no code) · **Acceptance:** G0 · **Status:** not started

Planning/decision work order — produces documents, not engine changes. It is the gate everything
else keys off: after WO-00, no required feature lacks an oracle, an owner, and a known execution
environment.

## Copy-paste prompt

~~~text
Execute only WO-00 from the Cosmic 2D stability packet. Read work-orders/README.md (global rules
+ locked decisions D-CPU/D-9km/D-commit), ../01-Repository-Review.md, ../02-Stability-Work-Orders.md
and the ../03-Acceptance-Test-Catalog.md bars. Revalidate the current HEAD SHA and the source
anchors you cite. This work order writes decision documents only — do not change engine code,
build config, or branches.

Produce, under docs/plans/2d-stability-2026-09-16/contracts/ :
1. support-matrix.md — the D-CPU SSE4.2 floor (cite JoltPhysics/CMakeLists.txt:44-45), Windows
   10/11 x64, OpenGL 4.5 core, >=16 GB RAM, NVIDIA incl. RTX 50-series; "qualified" vs "compatible"
   defined; exact models/drivers left as WO-02/WO-13 fields.
2. retained-feature-register.md — every M1-M12 item AND the shared surface the brief omitted
   (sprites/animation, tilemaps, 2D lighting, UI, scenes/prefabs, VFS/assets, scripts, graphs,
   events, jobs, audio, shared Jolt physics). For each: current behaviour, the acceptance ID(s)
   that cover it, the existing test(s) if any, and a class tag {verify | needs-testability |
   needs-fix | known-limitation}.
3. known-issues.md — formalize KI-1 (snap-chip abort, ViewportController.cpp:1284-1293), KI-2
   (SerialLink connected-state coverage gap), KI-3 (2D-enforcement/CI-cache gap), each with owner
   WO and a repro note. This is the register every later WO appends to.
4. contracts.md — draft contracts for: bounded serial close/cancel (deadline + thread/caller
   ownership), recording durability + max session/retention policy, the restricted numeric CSV
   grammar, runtime-plugin vs editor-module reload state, and numeric precision (float telemetry
   vs double scientific source). Mark each "draft — ratified with WO-02 measurements".
5. numeric-bar-policy.md — state that NO numeric bar in ../03 gates a merge until WO-02 measures a
   baseline for it on a named machine; list which bars are measurement-derived vs consumer-derived.
   D-9km means no consumer-derived bar is required this milestone.

Resolve the meaning of "main is 2D-only": recommended policy = COSMIC_2D_ONLY default ON on the
trunk PLUS configure-time rejection of OFF on the supported trunk, with OFF kept only on engine-3d.
Record Jolt as an explicitly retained dependency (not an optional backend to migrate).

Do not implement fixes. Commit the documents locally per D-commit (author kdadabhoy, no AI trailer).
Do not push. Return: the file list, the open contract questions that still need Kaden, and whether
every mandatory acceptance ID now has an owner and environment.
~~~

## Scope
- **In:** decision/contract documents; the three registers; the 2D-only policy recommendation.
- **Out:** any engine/build/test/branch change; measuring the numbers (that is WO-02).

## Deliverables
`contracts/support-matrix.md`, `retained-feature-register.md`, `known-issues.md`, `contracts.md`,
`numeric-bar-policy.md`.

## Done when (DoD)
No required feature lacks an oracle, a test owner/order and a known environment. Hardware/fixture
availability may remain pending, but its mandatory tests cannot disappear. KI-1/2/3 are recorded
with owners. D-CPU and D-9km are written into the support matrix and numeric-bar policy.

## Rollback
Documents only — revert the commit. No engine state to restore.
