# WO-06 — Recording, replay, CSV and write-failure integrity

**Gate:** G3 · **Depends on:** WO-04, WO-05 · **Acceptance:** D01–D06 · **Status:** not started

Prove the record → flush → load → replay → export path and the restricted CSV grammar under corrupt
input, concurrency and disk failure — with an independent oracle, no format change.

## Copy-paste prompt

~~~text
Execute only WO-06 from the Cosmic 2D stability packet. Read work-orders/README.md, ../02 (WO-06),
../03 (D01-D06 + the D06 CSV cases, fixtures F-RECORD/F-CORRUPT). Work in codex/2d-stability,
COSMIC_2D_ONLY=ON, via the WO-04 runner.

Primary paths: Cosmic/src/telemetry/DataRecorder.{h,cpp}, DataPlayer.{h,cpp},
Cosmic/src/utils/DataExport.cpp. Telemetry is float (TelemetryChannel.h:27); scientific source stays
double at the consumer. Recorder appends + snapshots for Flush; SF_Telem reserves k_RecordCap =
60*300 = 18000 samples (5 min @ 60 Hz), autosaves every 5 s (TelemHub.h:252-264) — Reserve is
preallocation, NOT a data-loss bound.

Do:
  - Pin real SF-Stable v1 recordings (when available) as immutable compatibility fixtures; hash them.
  - Verify exact stored float values, channel names, sample counts, timestamp order, scene.bin
    precedence over per-entity fallback, independent entity rates, timestamp interpolation,
    play/pause/seek/reverse, and isolation between loaded replay and live input (add the UI-command
    state coverage test_sftelem_hub.cpp:15-24 documents as uncovered — via production commands, not
    private-field mutation).
  - Corrupt/robustness: existing truncation/count guards + bounded F-CORRUPT fuzz; reject invalid
    input without unbounded allocation/hang; failed load leaves a documented state.
  - Write-failure injection: disk-full, permission-denied, invalid path, interrupted autosave,
    shutdown during flush. Preserve the last complete recording; never report a partial write as a
    valid full recording. MEASURE the actual data-loss window; do not assume the 5 s autosave bounds
    it.
  - CSV: keep the restricted numeric grammar (LoadCSV rejects ragged rows DataExport.cpp:266,
    non-numeric/blank cells :277/:225, has no quoted parser :211-221). Run the D06 matrix — blank
    cells not silently zero, numeric-looking headers, BOM/Unicode paths, NaN/Inf/overflow, locale,
    write/flush/close failures — each with an explicit supported/rejected contract.

No format change without a version/compatibility decision. Commit locally per D-commit. Do not push.
Return: D01-D06 results, the measured data-loss window, and the CSV grammar contract as asserted.
~~~

## Scope
- **In:** recorder/player robustness + real UI-command coverage; write-failure injection; the CSV
  edge matrix; compatibility fixtures.
- **Out:** general quoted-CSV / MATLAB-Horizons import (not chosen); to-9km schema (D-9km deferred);
  UDP.

## Deliverables
`tests/` additions for D01–D06; pinned `F-RECORD`/`F-CORRUPT` fixtures; the recording-durability +
CSV-grammar contracts in `contracts/`; measured data-loss window.

## Done when (DoD)
D01–D06; recorded value/sample accounting matches the independent fixture; no invalid file reported
as a valid full recording; the CSV grammar is asserted consistently. No silent format change.

## Rollback
Additive tests/fixtures + any focused fix — revert the commit.
