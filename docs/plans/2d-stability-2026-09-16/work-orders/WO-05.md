# WO-05 — COM close/disconnect reliability (the reported crash), full matrix

**Gate:** G3 · **Depends on:** WO-03, WO-04, WO-05a · **Acceptance:** T01–T06 · **Status:** not started

Ship the fix for the reported close/link-loss crash with regression proof, and cover the full serial
lifecycle matrix using the WO-04 seam and (where available) real Windows transport.

## Copy-paste prompt

~~~text
Execute only WO-05 from the Cosmic 2D stability packet. Read work-orders/README.md, WO-05a's repro +
seam, ../02 (WO-05) and ../03 (T01-T06 + the T03/T04 transition matrix). Work in codex/2d-stability,
COSMIC_2D_ONLY=ON, using the WO-04 acceptance runner and transport seam.

Primary paths: Cosmic/src/serial/SerialPort.{h,cpp}, SerialLink.{h,cpp}, SF_Telem OnDetach
(SF_Telem.cpp:84-93), TelemHub::Shutdown, and the host's deferred transition/shutdown. Test the
ACTUAL shared ownership chain (root-owned m_Link shared with the screens), not an isolated
SerialPort.

Land the fix for the WO-05a reproduction FIRST, with a failing-before test and a passing-after
result. Then cover the matrix with barriers (not timing guesses), >=100 deterministic iterations per
transition schedule: delayed open, abandoned successful open, pending read, silent stall, hard
disconnect, in-flight write, reconnect, close — crossed with app-window close, return-to-launcher,
connection-panel close, screen switching and recording/export states. State the permitted
caller/thread contract; do NOT "fix" a blocking shutdown by detaching a thread that still references
a destroyed object.

Preserve SF-Stable parser/firmware behaviour AND main's existing hardening; log any intentional
malformed-input rejection difference in the ledger. Where possible, corroborate fake-transport cases
on real Windows virtual COM (com0com) + representative USB/Bluetooth SPP hardware, both Windows
versions; >=20 iterations per physical disconnect/close scenario. A manual process-kill is NOT a
pass. KISS/CRC8 and PC-side tagged text are separate surfaces; COBS is neither.

Every worker/handle/callback balances; no deadlock/race/crash; close meets the approved deadline.
Commit locally per D-commit. Do not push. Return: the fix + regression, T01-T06 results, the
cancellation/ownership contract, and which rows are hardware-blocked vs proven.
~~~

## Scope
- **In:** the reported-crash fix + regression; the full T03/T04 lifecycle matrix; parity vs
  SF-Stable + retained hardening; real-Windows corroboration where hardware allows.
- **Out:** recording durability internals (WO-06); UDP (A2 backlog); the transport seam itself
  (WO-04).

## Deliverables
Serial fixes + `tests/` additions driven through the seam; the cancellation/thread-ownership
contract (into `contracts/`); T01–T06 evidence with fake vs virtual-COM vs device labelled.

## Done when (DoD)
T01–T06 including the user's close/link-loss scenarios; no leaked thread/handle/callback; a clear
bounded cancellation policy; failing-before evidence for each fix. Hardware gaps are explicit
`ENVIRONMENT_BLOCKED`, not passes.

## Rollback
Revert the serial commit; the seam and tests remain (they are additive) so the regression still
guards a re-fix.
