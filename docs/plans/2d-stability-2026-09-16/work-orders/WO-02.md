# WO-02 — Establish a fresh 2D baseline and the numbers the gates calibrate against

**Gate:** G1 · **Depends on:** WO-01 · **Acceptance:** B02, baseline reports · **Status:** not started

A clean, isolated 2D build + the honest current state, plus the **measured baselines** every
provisional numeric bar in `../03` is ratified from (review finding: no bar gates a merge until it
is measured here).

## Copy-paste prompt

~~~text
Execute only WO-02 from the Cosmic 2D stability packet. Read work-orders/README.md,
../02-Stability-Work-Orders.md (WO-02), ../04-Migration-Runbook.md §4 and ../03 provisional bars.
Work in the codex/2d-stability worktree. Configure EXPLICITLY with -DCOSMIC_2D_ONLY=ON.

Do not regenerate goldens. Do not treat the historical "340/340" as a result — discover the actual
2D test list and count.

Build:
  Debug and Release units (CosmicTests) with COSMIC_2D_ONLY=ON, then the GPU suite
  (CosmicRenderTests, COSMIC_BUILD_RENDER_TESTS=ON) on an available qualified GPU.
Run and capture:
  - exact CMake/compiler/SDK/dependency versions, CMakeCache, PUBLIC compile definitions;
  - the discovered 2D test names + counts (units and render), warnings, pass/fail;
  - GL renderer/version/driver, Windows build, CPU instruction features, RAM, machine identity;
  - tests/check_gl_conformance.ps1 and tests/check_docs_coverage.ps1 exit codes.
Baseline SF_Telem's supported workflows on current main AND on SF-Stable in SEPARATE runtime
directories (do not copy candidate DLLs into SF-Stable's folder). If the older build needs
unavailable tooling, record the blocker and keep source-parity evidence instead of claiming
runtime parity.

MEASURE the baselines the later gates need (record as "baseline", not pass/fail), each on the
named machine:
  - normal app/process close time from idle/open/stalled-port states;
  - UI-service heartbeat gap during a connection operation (p95/p99);
  - 10,000-instance frame time (Release, 1080p, vsync off, 10 s warmup + 60 s sample; p95/p99);
  - SF_Telem private-bytes curve over a short recording run (extrapolate the 2 h ceiling input);
  - non-recording memory plateau after a warmup/quiesce cycle.

Commit the baseline report locally per D-commit. Do not push. Return: the clean baseline report,
the actual 2D test count vs the 340 claim, existing failures left visible, and the measured numbers
WO-00's numeric-bar policy will ratify the ../03 bars from.
~~~

## Scope
- **In:** clean isolated 2D Debug/Release + GPU build; full environment capture; audits;
  SF_Telem baseline on main and SF-Stable; the measured baselines for the numeric bars.
- **Out:** fixing anything found (log it in `known-issues.md`); regenerating goldens; 3D builds.

## Deliverables
- `contracts/baseline-report.md` (or `evidence/WO-02/`): build identity, 2D test list + counts,
  audit results, SF_Telem workflow baseline, and the measured numbers table.

## Done when (DoD)
Clean baseline report with existing failures recorded (not hidden). The historical 340-test claim
is checked, not used as a success placeholder. Current GPU suite count recorded accurately. Every
provisional numeric bar has a measured baseline or an explicit "not measurable here" blocker.

## Rollback
Reporting/build only — discard the worktree build dir; no engine change to revert.
