# WO-13 — Qualify the pinned candidate, then hand Kaden the promotion

**Gate:** G5 · **Depends on:** all prior work · **Acceptance:** S01–S04, G5 · **Status:** not started

Run the complete catalog on the frozen candidate, collect the release report, and prepare — but do
not execute — promotion. The AI never promotes `main` or pushes.

## Copy-paste prompt

~~~text
Execute only WO-13 from the Cosmic 2D stability packet. Read work-orders/README.md, ../02 (WO-13),
../03 (S01-S04 + Definition of a passing campaign) and ../04-Migration-Runbook.md §6-§8. Work on
main at a PINNED commit; keep results tied to that exact SHA/diff, not a branch name.

Run the complete acceptance catalog on the pinned candidate after all code/packaging/docs changes:
both Windows versions, Debug/Release units, qualified GPU tests, actual host/editor workflows, real
Windows serial integration, 30-minute telemetry and two-hour soak sessions (S01 SF_Telem + S02
analysis sample). Separate a non-recording segment for the memory-plateau oracle. Use the WO-00
numeric bars now ratified against WO-02 baselines — do not loosen a bar to pass.

Collect a release report: exact SHA/diff status, feature/test/evidence traceability (M/S/T/D/L/R/C/
N/K/DOC IDs), qualified machines (exact NVIDIA models/drivers, CPU features, Windows builds), command
logs, skipped/blocked tests, crash/hang evidence, memory/resource/perf charts, package hashes,
durability results, known limits, and the KI-1..3 dispositions. Mandatory bugs stay blocking;
deferred A1-A5 + D-9km consumer qualification are listed separately, not as failures.

Prepare the milestone hand-off for Kaden (DO NOT run it): the campaign is main-only, so there is NO
merge/ff-only step — the "promotion" is simply Kaden pushing main at the qualified SHA and verifying
the required 2D CI passes on that exact commit, plus the branch-SHA/package-hash checks (runbook §7).
Verify the engine-3d snapshot + SF-Stable + the cosmic-pre-2d-2026-09-16 tag are durably recorded first.

Commit the release report locally per D-commit. Do not push, merge, promote main, or publish.
Return: the full requirement->case->evidence matrix, every blocked/failed case, and the exact
promotion commands staged for Kaden to run.
~~~

## Scope
- **In:** full catalog on the pinned candidate; both Windows; soaks; release report; staged
  promotion commands + pre-promotion durability checks.
- **Out:** executing the promotion/push/publish (Kaden's, per D-commit); new features; changing a
  ratified bar to pass.

## Deliverables
`evidence/WO-13/release-report.md` (full traceability matrix + qualified environments + dispositions)
and the staged, unexecuted promotion runbook commands.

## Done when (DoD)
Every mandatory case passed **or** Kaden explicitly changed the release scope; G5 satisfied; no claim
that an unexecuted/blocked test passed; promotion is staged for Kaden, not performed. The
engine-3d/SF-Stable/tag preservation is verified durable before promotion is offered.

## Rollback
Report-only WO. Promotion is a separate, Kaden-run action with its own runbook §7/§8 rollback (deploy
the previous qualified artifact + a reviewed revert; never rewrite history or move engine-3d).
