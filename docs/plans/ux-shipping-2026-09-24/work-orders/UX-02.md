# UX-02: One gizmo per selection, UI Active semantics, flow usage in the Inspector, Scenes list, Preferences

**Gate:** G1 · **Wave:** 1 (∥ UX-01, UX-03; lands second) · **Runs:** worktree `build\_lanes\ux-02`, branch
`ux/02` · **Base:** `main` after the packet commit (UX-00) · **Depends on:** UX-00 · **Acceptance:** ED01–ED05 ·
**Model:** Opus 5.5 · **Effort:** high · **Status:** not started

Kaden's items 16–19 and 21. Two gizmos overlap on UI elements and the one he grabbed moves nothing; covered
UI elements cannot be dragged; Rotate looks dead on sprites; a sprite under an opaque UI background cannot
be picked; the Hierarchy eyeball does not hide a UI element; the Inspector cannot say what a button does
when the flow handles it; there is no list of the project's scenes; autosave exists but has no UI and
nothing asks before unsaved work is dropped. This lane fixes the defects (KIs first), adds the three
features, and proves each one headless and through an editor self-test host.

## Copy-paste prompt

~~~text
Execute only UX-02 from the Cosmic "UX & Shipping" packet (docs/plans/ux-shipping-2026-09-24/).
Nobody answers questions: decide, proceed, report honestly (blocked/failed is reported as such).
Read first, and nothing else until you edit a file named below:
 1. work-orders/README.md (global rules, lane rules L1-L5, build commands, next free KI number)
 2. 01-Contracts.md §2 (viewport, UI, prefs), §10 (UX-02 row), §11 (register format)
 3. 03-Acceptance-Catalog.md rows ED01-ED05
 4. Projects/Starforge/src/ViewportController.cpp:216-340 and :572-629, :651-930 (+ .h)
 5. Projects/Starforge/src/UiRectGizmo.cpp:220-291 and Cosmic/src/graphics/Gizmo.cpp (+ Gizmo.h)
 6. Cosmic/src/scene/ui/UiSystem.cpp:300-430
 7. Projects/Starforge/src/SourceLocator.h + SourceLocator.cpp
 8. Projects/Starforge/src/EditorPrefs.h and AP03AuthoringSelfTest.cpp (the self-test pattern)
Names and signatures in 01-Contracts.md win over this prompt; any deviation goes in the report.

Lane: from C:\dev\Cosmic run  git worktree add build\_lanes\ux-02 -b ux/02 main  (main = the UX-00
commit), work only inside C:\dev\Cosmic\build\_lanes\ux-02, set $env:COSMIC_SDK to that folder, build
into <worktree>\build with the README commands (-DCOSMIC_2D_ONLY=ON -DCOSMIC_BUILD_TESTS=ON). UX-01 and
UX-03 run at the same time; UX-01 lands before you. StarforgeApp.cpp is at the /bigobj limit: new code
goes in new TUs (e.g. StarforgeAppPrefs.cpp, the StarforgeAppPlatform.cpp pattern), the shell gets hooks.
Revalidate: print git rev-parse HEAD, git status --short, cmake --version; re-check every anchor (0c2edd8).

KIs first (known-issues.md in docs/plans/2d-stability-2026-09-16/contracts/, template :14-22, numbers from
the next free number in work-orders/README.md, renumber at rebase if taken), each with failing-before on an
isolated stash, fix, passing-after:
(a) the transform gizmo draws for any entity with a Transform (ViewportController.cpp:594-595), including
    UI elements whose layout ignores it under a canvas (UiComponents.h:80-82): dragging moves nothing;
(b) the rect gizmo's Move surface captures only when the selection is topmost (UiRectGizmo.cpp:264-273);
(c) Gizmo::Manipulate writes RotationQuat + UseQuatRotation (Gizmo.cpp:89-92), sprites draw Rotation.z
    (Scene.cpp:716): Rotate is invisible on sprites;
(d) in edit mode the UI hit-test (ViewportController.cpp:255-266) runs before the sprite pick (:275), so
    a selected sprite under an opaque UiImage cannot be pressed or dragged;
(e) VisitUi (UiSystem.cpp:310-401) never checks TagComponent::Active (Components.h:90-94); only canvas
    roots are skipped (:415), so a hidden element still draws, takes clicks and hosts panels.

Changes (the §2 rules; anchors):
1. One gizmo. DrawGizmo (ViewportController.cpp:572-629) skips the transform gizmo when the primary
   selection has RectTransformComponent or CanvasComponent. The rect gizmo's Move captures the selected
   element whenever the pointer is on its centre square, whether or not it is topmost (§2 allows the whole
   rect; pick one, say which and why). 2D rotation: move the decomposition (Gizmo.cpp:74-93) into a pure
   static Gizmo::ApplyModel(TransformComponent&, const glm::mat4&, bool mode2D); in 2D it writes
   Rotation.z (degrees, from the matrix), keeps Rotation.x/y, leaves UseQuatRotation false and RotationQuat
   untouched; Manipulate (Gizmo.h:83-93) gains a defaulted mode2D argument that ViewportController passes
   in 2D (Gizmo.h is outside §10: record it). Pick: when the primary selection is a world entity with a
   SpriteRenderer, test its sprite bounds (the :278-296 math) before the UI hit-test, so the press keeps
   it selected and the gizmo drags it; one CommandStack entry per gesture (CommitTransform, :613-619).
   Toolbar chips that do not apply to a UI selection (ops :690-697, World/Local :713-730, snap chips
   :736-778, Frame selection :917-922) draw disabled with the tooltip "UI elements: use the rect gizmo";
   keep the chip slots and IDs the KI-1 harness relies on (:816).
2. Active. In VisitUi, right after the valid/visited check (:326-327), skip a node whose TagComponent is
   inactive and do not push its children; Render, Update, HitTest and CollectHostedPanels follow.
3. What a button does. SourceHit (SourceLocator.h:26-33) gains a kind (Source | Flow) and state/target
   fields; ForSignal (SourceLocator.cpp:210-224) appends one Flow hit per transition of the project's
   startup flow (startup_flow in <root>/project.cproj, read and parsed with FlowAsset::LoadFromString)
   whose On equals the signal: path = the .cflow, line 0. The Inspector Signal row (InspectorPanel.cpp:
   569-591, UiButton/UiSlider/UiToggle) and the viewport "Open logic source" menu (StarforgeAppPlatform.cpp:
   315-349) show each as a read-only line in the §2 format with "Open in flow editor": open the document
   the way ScreensHost does (StarforgeAppPlatform.cpp:139-142) and select that transition with
   FlowEditor::HarnessSelect (FlowEditor.h:54). E08's existing cases must stay green.
4. Scenes list. Screens panel (ScreensPanel.cpp; states table :220-255) gains a Scenes section: every
   project://scenes/**/*.cscene, recursive and sorted (never *.bak), start scene marked, open scene
   highlighted, double-click -> host.OpenScene, right-click -> Reveal. One pure lister shared with
   File ▸ Open Scene (StarforgeApp.cpp:2015-2031, today a flat directory_iterator at :2021).
5. Preferences. EditorPrefs.h: keep the key autosave_minutes (today float AutosaveMinutes = 5.0f, :35,
   read :213, written :237) as whole minutes 1-60, default 5, reading a legacy float; add autosave_enabled
   (bool, true) and prompt_unsaved (bool, true) in user://starforge/editor.toml. Split LoadSettings /
   SaveSettings (:206-251) into path-taking halves so a test never touches the real editor.toml. Edit ▸
   Preferences… (Edit menu :2064-2090) opens a modal with the three controls, the autosave folder
   user://starforge/autosave/ and a Reveal button. Autosave (StarforgeApp.cpp:1296-1314) obeys the flag
   (the pre-Play safety copy at :603-609 stays; its tooltip says so); DrawStatusBar (:2168-) shows
   "autosaved HH:MM". When Dirty and prompt_unsaved, the user commands behind NewScene (:528), OpenScene
   (:545), OpenProject (:239, :272), Close Project (:2057) and Exit to Launcher (:2059-2060) raise Save /
   Discard / Cancel in the shape of AssetEditorHost.cpp:131-179; Cancel aborts. Wrap the commands, not
   the functions: self-tests call CloseProject() directly (AP03AuthoringSelfTest.cpp:842, :892). The OS
   and chrome close end the app before any layer sees the event (Application.cpp:471, :684-688;
   WorkspaceLayer.cpp:294): write the autosave copy in OnDetach (:144-193) when dirty and record the
   missing vetoable close as a contract deviation. After rebasing onto UX-01, include
   m_Editors.AnyDirty() in the dirty test.

Tests, named "UX-02 EDnn ...": ED02 in tests/test_ui_widgets.cpp beside :536-548 (inactive UiPlot child:
no draw, Update false and silent, HitTest skips, hosted panel omitted; re-activate restores); ED01 unit
(ApplyModel, 30 deg Z: Rotation.z = 30 +- 1e-4, UseQuatRotation false, quat untouched), ED03 (ForSignal
("back_clicked") on COSMIC_PENDULUMLAB_DIR gives the Flow hit Settings -> Lab plus src hits), ED04
(lister on a fixture tree), ED05 (prefs round trip, defaults when absent) in tests/test_ap03_editor.cpp.
Fixture project tests/fixtures/ux02/ (committed): scenes/overlays/Pause.cscene, a Main.cscene.bak, and a
scene with one sprite under a full-screen opaque UiImage.
Self-test host Projects/Starforge/src/UX02EditorSelfTest.cpp (COSMIC_UX02_SELFTEST=<result.json>,
COSMIC_UX02_ROOT, COSMIC_UX02_SHOTS; COSMIC_STARFORGE_PROJECTS_DIR, StarforgeAppPlatform.cpp:61-63),
hooked beside the AP03/GUIDE hooks (StarforgeApp.cpp:139-140, :188-189, :1021-1022, :1662-1663;
StarforgeApp.h:379-395); real commands, injected pointer input (the E03 / L05 pattern), the WO07 stack
oracle. ED01: App template Dashboard, select Plot -> no transform gizmo (probe counter), rect gizmo drawn;
fixture sprite scene -> press-drag (40,-20) px on the sprite moves Position by the world equivalent, one
CommandStack entry, undo restores. ED03: Inspector on PendulumLab's Settings Back button shows the Flow
line; Open in flow editor opens Main.cflow (after the rebase, UX-01's HarnessSelection confirms the
transition). ED04: Scenes section and File ▸ Open Scene list the fixture set; double-click opens.
ED05: autosave off -> 2x the interval of injected time through the real Autosave writes nothing; on ->
one file and the status text; prompt on + dirty -> OpenScene command shows the modal, Cancel keeps,
Discard switches; prompt off -> switches. Wrapper tests/acceptance/fixtures/Run-UX02Editor.ps1
(Run-AP03Authoring.ps1 pattern); manifest tests/acceptance/manifests/ux02-editor.manifest.json, ED01-ED05.

Evidence: docs/plans/ux-shipping-2026-09-24/evidence/UX-02/report.md in the WO-10 layout (scope and
provenance, toolchain, what was built, status per ID, defects registered before fixing, measured numbers,
caveats, files, local commits); commit *-excerpts.txt and result JSON, *.log stays ignored. §11 rows:
one-gizmo rule, UI Active semantics, flow-usage hits, Scenes list, prefs keys.

Land (L2): git rebase main (UX-01 is in; keep both sides in StarforgeApp.cpp/.h, tests/CMakeLists.txt and
the KI register); rebuild Debug + Release, 0 warnings; CosmicTests both configs (>= the base count plus
yours); Run-Acceptance.ps1 -Manifest <absolute ux02-editor path> -Config Debug|Release -TempRoot
<worktree>\build\_temp\ux02; retained ap03-editor (E03, E08), wo07-ki1, wo07-l02, wo09-editor,
wo10-sample; check_gl_conformance, check_docs_coverage, check_docs_links exit 0. Stage explicit paths,
commit on ux/02 as kdadabhoy <kdadabhoy28@gmail.com>, no Co-Authored-By, no AI trailer: KIs, gizmo +
pick, Active, flow usage, Scenes, prefs + prompt, self-test. Never push.
Report (<= 40 lines): ED01-ED05 per config, KI numbers, the Move-capture choice, contract deviations,
SHAs, git status --short.
~~~

## Files to read first (and nothing else)

`work-orders/README.md`; `01-Contracts.md` §2, §10, §11; the ED catalog rows;
`ViewportController.cpp:216-340, 572-629, 651-930` (+ `.h`); `UiRectGizmo.cpp:220-291`; `Gizmo.{h,cpp}`;
`UiSystem.cpp:300-430`; `SourceLocator.{h,cpp}`; `EditorPrefs.h`; `AP03AuthoringSelfTest.cpp`.

## Owns / May touch

See `01-Contracts.md` §10 (UX-02 row). **Owns:** `ViewportController.{h,cpp}`, `UiRectGizmo.{h,cpp}`,
`panels/{HierarchyPanel,InspectorPanel,ScreensPanel}.{h,cpp}`, `EditorPrefs.h`, `SourceLocator.{h,cpp}`,
`Cosmic/src/graphics/Gizmo.cpp` (+ `Gizmo.h` for `ApplyModel`, recorded as a deviation),
`Cosmic/src/scene/ui/UiSystem.cpp` (`VisitUi` active check only), the new `StarforgeAppPrefs.cpp` and
`UX02EditorSelfTest.cpp`, `tests/test_ui_widgets.cpp` and `tests/test_ap03_editor.cpp` (added cases),
`tests/fixtures/ux02/**`, `tests/acceptance/fixtures/Run-UX02Editor.ps1`,
`tests/acceptance/manifests/ux02-*.json`, `evidence/UX-02/**`.
**May touch:** `StarforgeApp.cpp` (Autosave `:1296-1314`; the New/Open/Close/Exit command sites; Edit menu
item; File ▸ Open Scene `:2015-2031`; the status-bar hook; `OnDetach` autosave line; the four self-test
hooks); `StarforgeApp.h` (hook declarations, prompt state); `StarforgeAppPlatform.cpp:315-349`;
`tests/CMakeLists.txt`; the KI register (append).

## Scope

- **In:** items 16, 17, 18, 19, 21 as listed; ED01–ED05.
- **Out:** coding logic in the Inspector (rejected on purpose: logic is C++ plus the flow; UX-D1's guide
  says why); a vetoable window close (engine change, report it); the flow editor itself (UX-01); guide
  pictures (UX-D1).

## Deliverables

The five KI fixes; flow-usage hits; the Scenes section and recursive Open Scene; Edit ▸ Preferences, the
status-bar timestamp and the unsaved-changes prompt; tests; `tests/fixtures/ux02/`; the self-test host,
wrapper and `ux02-editor` manifest; `evidence/UX-02/report.md`; §11 rows.

## Done when (DoD)

ED01–ED05 pass on Debug and Release; E03/E08 and the retained editor manifests (incl. `wo07-ki1`) still
pass; every KI has failing-before and passing-after evidence; prefs round-trip in `editor.toml`; both
audits and the link checker exit 0.

## Rollback

Revert the lane's commits in reverse order; each change group is its own commit. Prefs keys are additive
(an older editor ignores `autosave_enabled` / `prompt_unsaved`).
