# UX-04 — SDK release zip, Starforge installer, release workflow, GLFW install leak, KI-63 + app-repo packaging KI

**Gate:** G2 · **Wave:** 2 (worktree; alongside UX-G0 and UX-D2) · **Runs:** worktree `ux/04` at `build\_lanes\ux-04` ·
**Base:** `main` after UX-01 → UX-02 → UX-03 have landed (post-wave-1) · **Depends on:** wave 1 (UX-03's sample
discovery lists the zip's `Projects/PendulumLab`) · **Acceptance:** SD01–SD04, K02-bak · **Model:** Opus 5.5 ·
**Effort:** high · **Status:** not started

D-SHIP made real: `Cosmic-SDK-<ver>-win64.zip` (checkout-mirror), a per-user `Starforge-Setup-<ver>.exe`, a tag-triggered
`sdk` job, the GLFW leak closed, KI-63 fixed recursively, and the app-repo packaging KI (no `extern/`; `dist/` under the project).

## Copy-paste prompt

~~~text
Execute only UX-04 from the Cosmic UX & Shipping packet (docs/plans/ux-shipping-2026-09-24/). Read ONLY:
work-orders/README.md (all); 01-Contracts.md §4 + your §10 row; 03-Acceptance-Catalog.md fixtures F-SDK/F-APPREPO and
rows SD01-SD04, K02-bak; installer/Stage-AppPackage.ps1; Projects/Starforge/src/Packager.cpp; installer/AppSetup.iss;
.github/workflows/release.yml; tests/acceptance/fixtures/Run-GuideWalkthrough.ps1. Anything else only at a named anchor.

Lane: from C:\dev\Cosmic  git worktree add build\_lanes\ux-04 -b ux/04 <post-wave-1 main SHA> ; work only there, $env:COSMIC_SDK
= that path (SD01 removes it); never touch UX-G0/UX-D2 files (UX-G0 owns StarforgeApp.cpp:1181-1197). Build the SDK in a
SECOND worktree (git worktree add build\_lanes\ux-04-sdk --detach ux/04) so the lane's build\ keeps tests ON
(CMakePresets.json:15). Anchors are from 0c2edd8: re-check each first (README rule 2). Version.h:20 = "0.9.0".

Do, in order, each its own commit:
1. KIs first (docs/plans/2d-stability-2026-09-16/contracts/known-issues.md; "the next free number" in work-orders/
   README.md, never hardcoded). KI-63 owner -> UX-04; cause: SkipContentEntry (Packager.cpp:21-27) and $skip
   (Stage-AppPackage.ps1:111) match TOP-LEVEL names, then :44-56 / :112-116 copy recursively, so scenes/*.cscene.bak
   (SceneSerializer.cpp:408-426) ships. NEW KI (app-repo packaging): the packager takes an external project's root as
   content (StarforgeApp::BeginPackage :2953-3060, :2985-2987), nothing skips extern/, dist/, .gitmodules, build.bat, and
   the output goes to <SdkDir>/dist (:2970) = extern/Cosmic/dist for an app repo (§5), so the copy recurses into its own
   output. Failing-before with the CLI stager only (-OutDir outside the content dir); never the editor on the unfixed tree.
2. K02-bak regression: scenes/Main.cscene.bak in the fixture project; the editor list (Run-Y02Package.ps1: plant after :89-94,
   payload :185-188) and the Stage-AppPackage.ps1 list (Run-PackageInstall.ps1 K02 :103-140, same copy) are identical, no .bak;
   same for a hand-made F-APPREPO-shaped copy (dummy extern/ - never a real SDK -, dist/, .gitmodules, build.bat): none of
   those staged, dist/ under the project. On the unfixed tree both FAIL: keep that run as failing-before.
3. Fix: skip every FILE *.bak at any depth plus extern, dist, .gitmodules, build.bat beside the existing seven, in BOTH
   Packager.cpp (recursive walk) and Stage-AppPackage.ps1 (recursive filter; PS 5.1, ASCII). An external project packages
   to <project root>/dist/<App> (BeginPackage :2970); in-tree projects (no own root, e.g. Starforge) keep <SdkDir>/dist
   (record it). Follow it in the popup text (:2909), RunStandalone (:3104-3128) and Packager.cpp's .iss/zip paths (:255,
   :268, :336). X01/Y02 drivers read dist from their JSON; report Run-GuideWalkthrough.ps1:27 and AP-Q1's
   Run-K02PendulumLab.ps1:10 (both hard-code <repo>\dist) to UX-D1/UX-Q1, don't edit them. Re-run K02-bak (pass),
   pr-units, apq1-y02 (copy its results to evidence/UX-04/, then git checkout -- docs/plans/app-platform-2026-09-18).
   Close both KIs.
4. Cosmic/CMakeLists.txt above add_subdirectory(dependencies/glfw) (:30; GLFW_INSTALL defaults ON, glfw/CMakeLists.txt:13):
   set(GLFW_INSTALL OFF CACHE BOOL "" FORCE). Proof: cmake --install to a temp prefix loses include/GLFW and lib/glfw3*.
5. installer/Stage-Sdk.ps1 (PS 5.1, ASCII; -SdkRoot -OutDir -Configs Release,Debug -ListOut -Zip -Build [-AllowDirty]),
   producing exactly the §4 tree. Selective parts: Cosmic/src/**/*.h; imgui/*.h + imgui.cpp, imgui_draw.cpp,
   imgui_widgets.cpp, imgui_tables.cpp; implot/*.h + implot.cpp, implot_items.cpp; glm/glm/**/*.{hpp,inl,h};
   entt/src/**/*.{hpp,h}; spdlog/include/**/*.h; Projects/PendulumLab/** minus build/; installer/licenses/**,
   Stage-AppPackage.ps1, AppSetup.iss; every source path installer/licenses/MANIFEST.txt names, at that path; sdk.toml
   (version, sha = HEAD of the staged commit, date ISO-8601, layout = "checkout-mirror"; dirty tree refused unless
   -AllowDirty, then sha ends "-dirty"); VERSION; README-SDK.md from a new installer/README-SDK.md (unzip, set nothing, run
   build\Runtime\Release\Starforge.exe; VS "Desktop development with C++"; the consumer recipe; the editor's data).
   build/Runtime/{Release,Debug} are MIRRORED minus the §4 exclusion list, never an allow-list (PDBs, .exp, .lib but
   Cosmic.lib, CosmicTests*/CosmicRenderTests*, *Fixture*.dll, WO07NoExport.dll, SF_Telem.dll + assets/projects/SF_Telem/**,
   assets/cache/**, sample output, CMake files, starforge/ user data; a dev tree's logs/, imgui.ini, .ilk likewise, recorded).
   branding/** stays. Add the Release CRT DLLs of CMAKE_INSTALL_SYSTEM_RUNTIME_LIBS (root CMakeLists.txt:100-101; e.g. from
   a cmake --install to a temp prefix); the Debug CRT is not redistributable, so Debug gets none - say so. Then verify every
   MANIFEST.txt path exists in the tree (fail otherwise). Zip via Compress-Archive (package.bat:158-166) + a sorted list.
   -Build = configure + build Debug and Release with -DCOSMIC_2D_ONLY=ON -DCOSMIC_BUILD_TESTS=OFF
   -DCOSMIC_SKIP_PROJECTS="AnalysisSample;PendulumLab;SF_Telem", marked "# UX-05 replaces this with tools\Build-Sdk.ps1".
   package.bat: first argument `sdk` -> Stage-Sdk.ps1 before any other logic (-Build unless COSMIC_STAGE_ONLY=1).
6. installer/StarforgeSetup.iss from AppSetup.iss: AppName Starforge, own AppId, DefaultDirName={autopf}\Cosmic SDK,
   PrivilegesRequired=lowest, OutputBaseFilename=Starforge-Setup-{#AppVersion}, Source = the staged SDK dir, Start-menu +
   optional desktop shortcut to {app}\build\Runtime\Release\Starforge.exe, WorkingDir = that folder, optional task
   writing HKCU\Environment COSMIC_SDK={app} (uninsdeletevalue; ChangesEnvironment=yes), no .cham association,
   [UninstallDelete] so no {autopf}\Cosmic SDK survives (SD04). The folder is writable, so user:// is the editor's exe dir
   (FileSystem.cpp:100-102): prefs, layouts, autosaves, project registry live in {app}\build\Runtime\Release\starforge\ and
   go with the folder - the final page says so. No ISCC here: locally ENVIRONMENT_BLOCKED (Inno Setup 6); install nothing.
7. release.yml (today one `package` job, workflow_dispatch, required app_name, :16-22): new job `sdk` (windows-latest,
   permissions contents: write) on push of tags cosmic-sdk-v* and on workflow_dispatch when a new boolean input (default
   false) is set; `package` gets if: github.event_name == 'workflow_dispatch'. Steps: checkout; fail unless the tag is
   cosmic-sdk-v<COSMIC_VERSION_STRING>; Stage-Sdk.ps1 -Build -Zip; choco install innosetup (the job installs Inno Setup 6
   itself) + ISCC StarforgeSetup.iss; upload zip, installer, list; for a tag attach both to its GitHub Release. Kaden only.
8. docs/installer-guide.md: "The Cosmic SDK and the Starforge installer" (zip vs installer, data, release = push the tag).

Acceptance (driver tests/acceptance/fixtures/Run-UX04Sdk.ps1, manifest tests/acceptance/manifests/ux04-sdk.manifest.json):
- SD01: stage the zip from the lane's Release build, unzip to build\_temp\ux04\sdk (F-SDK), remove COSMIC_SDK from the
  child env, start <sdk>\build\Runtime\Release\Starforge.exe (cwd = its folder) armed for GuideWalkthroughSelfTest
  (GuideWalkthroughSelfTest.cpp:1-18; COSMIC_GUIDE_ROOT = build\_temp\ux04\projects, _REF = the checkout's
  Projects\PendulumLab\src): New Project (App) -> BuildScripts (every cmake line carries -DCOSMIC_SDK_DIR=<sdk>) -> Play
  reaches Home -> Package; run the packaged exe from build\_temp\ux04\elsewhere (copy Run-GuideWalkthrough.ps1:56-167; it
  pins COSMIC_SDK at :36 and is UX-D1's): log names the project, Home > Start reaches Lab, exits on CloseMainWindow (a kill
  is FAIL). The guide's project is PendulumLab2 (:258) and carries no Y02SelfTest (:835-840), so for the catalog's "SdkApp"
  and "Y02-style signal" add a leg: X01PackageSelfTest packages a copy of <sdk>\Projects\PendulumLab and its packaged exe
  passes COSMIC_Y02_SELFTEST from build\_temp\ux04\elsewhere (Run-Y02Package.ps1 pattern). Record both deviations.
- SD02: templates/app from the zip (@PROJECT_NAME@ -> SdTwo), cmake -B build\_temp\ux04\plain -A x64 -DCOSMIC_SDK_DIR=<sdk>
  -DGAME_OUTPUT_DIR=..., Release + Debug; each DLL loaded by the SAME config's <sdk>\build\Runtime\<cfg>\CosmicApp.exe
  --project <abs dll> (KI-34) prints its module registration.
- SD03: the sorted zip list equals evidence/UX-04/sdk-files.txt (committed once here); no *.pdb, *.exp, CosmicTests*,
  *Fixture*, WO07NoExport*, SF_Telem*, CMakeCache.txt, assets/cache/**; has Projects/PendulumLab/project.cproj, every
  MANIFEST.txt LICENSE path and the CRT DLLs; sdk.toml sha = the staged commit; size recorded, not gating.
- SD04: on a disposable Windows 10/11 VM without Visual Studio: installs, the shortcut opens the homescreen, New Project
  logs the missing toolchain honestly, uninstall leaves no {autopf}\Cosmic SDK. ENVIRONMENT_BLOCKED (prerequisite named)
  without a VM - never enable Windows Sandbox or any Windows feature. K02-bak: steps 2-3.

Evidence: evidence/UX-04/report.md (WO-10 layout), failing-before/, sdk-files.txt, zip hash, K02 lists, results.json, zip and
editor log excerpts; §11 rows; for UX-D3: building-and-shipping.md "Which install() rules exist" is stale (do not edit).
Land (L2): git rebase main; Debug + Release 0-warn; CosmicTests both; ux04-sdk, pr-units, apq1-y02; both audits + links
checker; commit on ux/04 as kdadabhoy <kdadabhoy28@gmail.com>, no Co-Authored-By / AI trailer; never push, tag or trigger
a workflow. Report <= 40 lines: verdicts, zip size/hash/count, KIs, deviations, SHAs, git status --short.
~~~

## Files to read first (and nothing else)

The eight in the prompt: README; §4 + §10 row; catalog fixtures + SD/K02-bak rows; stager, packager, `.iss`, workflow, guide driver.

## Owns / May touch

- **Owns:** `installer/**` (new `Stage-Sdk.ps1`, `StarforgeSetup.iss`, `README-SDK.md`); `package.bat`; `release.yml`;
  `Packager.cpp` (skip rule + dist paths); `Cosmic/CMakeLists.txt` (`GLFW_INSTALL`); `docs/installer-guide.md`; `Run-UX04Sdk.ps1`;
  `manifests/ux04-*.json`; K02 `.bak` lines in `Run-PackageInstall.ps1`/`Run-Y02Package.ps1`; `evidence/UX-04/**`.
- **May touch:** `StarforgeApp.cpp` `:2909`, `:2953-3060`, `:3104-3128` only; the KI register; §11 rows; `pr-*` re-runs.

## Scope

- **In:** zip + staging script, installer, `sdk` job, `package.bat sdk`, GLFW leak, KI-63 + the app-repo packaging KI.
- **Out:** `sdk` preset / `Build-Sdk.ps1` / app-repo kit (UX-05); guides and their driver (UX-D1); template, `SdkDir()`; any push.

## Deliverables

Installer files, `package.bat sdk`, `sdk` job, skip + dist fix, `GLFW_INSTALL OFF`, SD driver + manifest, `sdk-files.txt`, KIs.

## Done when (DoD)

Zip = `sdk-files.txt`; SD01–SD03 pass; SD04 passed or blocked; editor = CLI lists, no `.bak`/`extern`; KIs closed; checkers 0.

## Rollback

Revert the lane's commits in reverse; each (KI fixes, GLFW line, staging script, installer, workflow, docs) reverts alone.
