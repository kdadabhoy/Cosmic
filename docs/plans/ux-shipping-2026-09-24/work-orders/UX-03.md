# UX-03: Launcher hygiene, samples from disk incl. PendulumLab, welcome, App default

**Gate:** G1 · **Wave:** 1 (∥ UX-01, UX-02; lands third) · **Runs:** worktree `build\_lanes\ux-03`, branch
`ux/03` · **Base:** `main` after the packet commit (UX-00) · **Depends on:** UX-00 · **Acceptance:** LH01–LH02 ·
**Model:** Opus 5.5 · **Effort:** high · **Status:** not started

Kaden's items 20 and 4. The Cosmic Launcher (`CosmicApp.exe`) lists every DLL next to it that exports
`CreatePluginLayer`, so the test fixtures the suite builds into `build/Runtime/<cfg>` appear under "Engine
demos & tools". Starforge's homescreen offers only the two template samples (FlowDemo, ForgePong), the
one-time welcome offers ForgePong, and New Project defaults to Game, while the sample Kaden actually uses,
PendulumLab, is reachable only through Open…. This lane hides fixtures by structure (D-LAUNCHER: a marker
export, no name list), lists samples from both sources grouped by kind with PendulumLab featured
(D-SAMPLES), and makes App the default.

## Copy-paste prompt

~~~text
Execute only UX-03 from the Cosmic "UX & Shipping" packet (docs/plans/ux-shipping-2026-09-24/).
Nobody answers questions: decide, proceed, report honestly (blocked/failed is reported as such).
Read first, and nothing else until you edit a file named below:
 1. work-orders/README.md (global rules, lane rules L1-L5, build commands, next free KI number)
 2. 01-Contracts.md §3 (launcher and samples), §10 (UX-03 row), §11 (register format)
 3. 03-Acceptance-Catalog.md rows LH01-LH02
 4. Cosmic/src/layers/LauncherLayer.cpp:340-400 and :578-634 (+ LauncherLayer.h)
 5. Cosmic/src/scripting/ModuleMacros.h and tests/CMakeLists.txt:166-256
 6. Projects/Starforge/src/StarforgeAppPlatform.cpp:40-70 and :351-473
 7. Projects/Starforge/src/StarforgeApp.cpp:108-116, :297-325, :2436-2477, :2696-2762
 8. Projects/Starforge/src/AP03AuthoringSelfTest.cpp:835-900 (the E06 block you extend)
Names and signatures in 01-Contracts.md win over this prompt; any deviation goes in the report.

Lane: from C:\dev\Cosmic run  git worktree add build\_lanes\ux-03 -b ux/03 main  (main = the UX-00
commit), work only inside C:\dev\Cosmic\build\_lanes\ux-03, set $env:COSMIC_SDK to that folder, build
into <worktree>\build with the README commands (-DCOSMIC_2D_ONLY=ON -DCOSMIC_BUILD_TESTS=ON). UX-01 and
UX-02 run at the same time; both land before you. No new .cpp under Cosmic/src (no re-configure needed).
Revalidate: print git rev-parse HEAD, git status --short, cmake --version; re-check every anchor (0c2edd8).

KI first (known-issues.md in docs/plans/2d-stability-2026-09-16/contracts/, template :14-22, number from
the next free number in work-orders/README.md, renumber at rebase if taken): the Launcher lists the test
fixtures under "Engine demos & tools" (LauncherLayer.cpp:365): ScanForProjects (:582-634) scans the exe
dir and <exe>/projects (:590-591) for any DLL exporting CreatePluginLayer (:619-623), and
tests/CMakeLists.txt:176-254 builds ten fixture DLLs into build/Runtime/<cfg>. Failing-before = LH01's
unit on the unfixed code (on an isolated stash), then the fix, then passing-after.

Changes:
1. Marker. ModuleMacros.h (after CS_MODULE_END, :106-120) gains exactly
     #define CS_TEST_FIXTURE() extern "C" __declspec(dllexport) int CosmicTestFixture() { return 1; }
   Invoke it once at file scope in the nine tests/*Fixture.cpp (WO05HostFixture, WO06HostFixture,
   WO07LifetimeFixture, WO07TeardownFixture, WO07ModuleFixture, WO07PlotFixture, WO07UiCyclesFixture,
   AP01ServiceFixture, WO10ClockFixture), after CS_MODULE_END() where a file uses it. WO07NoExport.cpp
   links nothing and exports no entry point: leave it (it stays hidden). Output dirs and the tests that
   load the fixtures by name do not change.
2. Launcher. Add a static, COSMIC_API-exported overload
     std::vector<std::string> LauncherLayer::ScanForProjects(const std::vector<std::filesystem::path>& dirs)
   holding today's loop plus: skip a DLL whose GetProcAddress(hMod, "CosmicTestFixture") is non-null, one
   CS_CORE_TRACE line per skipped DLL (Log.h:109), and one CS_CORE_INFO line listing what was found. The member
   ScanForProjects() (LauncherLayer.h:27) calls it with {exe/projects, exe}. LauncherLayer.h is outside the
   §10 row: record it.
3. Samples (§3). ListSamples() (StarforgeAppPlatform.cpp:379-390) returns SampleInfo {Name, Kind,
   Description, SourcePath, Featured} from assets/projects/Starforge/templates/samples/* (:382) and from
   <SdkDir()>/Projects/* holding a project.cproj, never Starforge (SdkDir(): StarforgeApp.cpp:437-447).
   Kind = the raw kind key (ProjectManifest::Load maps an absent key to "game", ProjectManifest.h:21);
   Description = the first non-title README.md line, skipping an @PROJECT_NAME@ line (FirstLine,
   StarforgeAppPlatform.cpp:43-55). DrawSampleButtons (:421-431, called from DrawHomescreen at
   StarforgeApp.cpp:2756-2758) draws "App samples", "Game samples", then "Other samples", PendulumLab
   first, marked featured, with a thumbnail: a <= 480 px wide
   copy of docs/showcase/07-lab-screen-live.png at Projects/Starforge/assets/editor/samples/PendulumLab.png
   (a new asset; record it). OpenSample (:403-419) keeps copying into SamplePath(<Name>) (:392-395, the
   Documents/Starforge Projects folder or COSMIC_STARFORGE_PROJECTS_DIR, :61-63) on first use:
   templates through ScaffoldProjectTo (StarforgeApp.cpp:297-325); Projects/* by a plain recursive copy
   that skips build/, dist/, logs/ and .starforge/ and seeds <dest>/.starforge/thumb.png from the
   thumbnail when one exists (ThumbFor reads it, StarforgeApp.cpp:2593-2610). The SDK copy is never
   edited in place. A project with no kind key (AnalysisSample, Projects/AnalysisSample/project.cproj) is
   listed last under a third group "Other samples" and is excluded from LH02's build/play loop (§3).
4. Welcome and default. The offer (StarforgeApp.cpp:112-115) tests SampleExists("PendulumLab"); the popup
   (:2439-2477) describes PendulumLab (a pendulum lab app: screens, a service on the DataBus, bound
   widgets, a hosted ImPlot panel) and its button calls OpenSample("PendulumLab"); keep the
   playground_offered key (EditorPrefs.h:37; UX-02 owns that file, add no key). New Project preselects App:
   m_NewProjectKind (StarforgeApp.h:376) = "app"; the picker (StarforgeAppPlatform.cpp:433-453) is
   unchanged otherwise.

Tests. tests/test_launcher_scan.cpp, cases "UX-03 LH01 ...": copy Starforge.dll (a real plugin,
StarforgeApp.cpp:3398) and AP01ServiceFixture.dll from the exe dir into a temp dir -> the overload returns
exactly {"Starforge"}; every *Fixture.dll in the exe dir exports CosmicTestFixture; WO07NoExport.dll is not
listed; a missing source DLL is a failure, never a skip. LH01 W: tests/acceptance/fixtures/
Run-UX03Launcher.ps1 starts CosmicApp.exe with no arguments and CWD = the runtime dir (the scan uses the
CWD, LauncherLayer.cpp:590), waits for the found-list log line, asserts Starforge and SF_Telem are in it
and no *Fixture name, then closes the process. LH02: extend E06 in AP03AuthoringSelfTest.cpp (:839-896)
without dropping its old oracle: the samples check (:848-850) expects App samples = [PendulumLab
(featured, first)] and Game samples containing FlowDemo and ForgePong; e06Kinds (:851) adds
"sample:PendulumLab" (scaffold to SamplePath, open, BuildScripts, Play); the welcome popup names
PendulumLab; the New Project dialog opens with App selected; LoadProjects <= 3 over 600 frames stays.
Manifest tests/acceptance/manifests/ux03-launcher.manifest.json: LH01-U (Run-WO10Case.ps1 -TestCase
"UX-03 LH01*"), LH01-W (the wrapper), LH02 (Run-AP03Authoring.ps1 with -Output under evidence/UX-03).

Evidence: docs/plans/ux-shipping-2026-09-24/evidence/UX-03/report.md in the WO-10 layout (scope and
provenance, toolchain, what was built, status per ID, defects registered before fixing, measured numbers
incl. the launcher lists before/after, caveats, files, local commits); commit *-excerpts.txt and JSON,
*.log stays ignored. §11 rows: fixture marker, sample discovery.

Land (L2): git rebase main (UX-01 and UX-02 are in; keep both sides in StarforgeApp.cpp/.h, the self-test
files, tests/CMakeLists.txt and the KI register); rebuild Debug + Release, 0 warnings; CosmicTests both
configs; Run-Acceptance.ps1 -Manifest <absolute ux03-launcher path> -Config Debug|Release -TempRoot
<worktree>\build\_temp\ux03; the fixture-loading manifests ap01-units, wo05-host-shards, wo06,
wo07-l01, wo07-l03, wo07-l04, wo07-l05, wo07-p01, wo10-host, and ap03-editor, wo10-sample;
check_gl_conformance, check_docs_coverage, check_docs_links exit 0. Stage explicit paths, commit on ux/03
as kdadabhoy <kdadabhoy28@gmail.com>, no Co-Authored-By, no AI trailer: KI, marker + launcher, samples,
welcome + default, E06 + manifest. Never push.
Report (<= 40 lines): LH01/LH02 per config, the KI number, the launcher list before/after, the three
sample groups as listed, deviations, SHAs, git status --short.
~~~

## Files to read first (and nothing else)

`work-orders/README.md`; `01-Contracts.md` §3, §10, §11; the LH catalog rows; `LauncherLayer.cpp:340-400,
578-634` (+ `.h`); `ModuleMacros.h`; `tests/CMakeLists.txt:166-256`; `StarforgeAppPlatform.cpp:40-70,
351-473`; `StarforgeApp.cpp:108-116, 297-325, 2436-2477, 2696-2762`; `AP03AuthoringSelfTest.cpp:835-900`.

## Owns / May touch

See `01-Contracts.md` §10 (UX-03 row). **Owns:** `Cosmic/src/layers/LauncherLayer.cpp` (+ `.h` for the
static overload, recorded as a deviation); `Cosmic/src/scripting/ModuleMacros.h` (the marker macro only);
`tests/*Fixture.cpp` (the marker line only); `tests/test_launcher_scan.cpp`;
`StarforgeAppPlatform.cpp` (`ListSamples`, `SamplePath`, `SampleExists`, `OpenSample`, `DrawSampleButtons`,
the template default); `Projects/Starforge/assets/editor/samples/PendulumLab.png` (new);
`tests/acceptance/fixtures/Run-UX03Launcher.ps1`; `tests/acceptance/manifests/ux03-*.json`;
`evidence/UX-03/**`. **May touch:** `StarforgeApp.cpp` (the offer `:112-115`, the popup `:2439-2477`, the
homescreen sample call `:2756-2758`); `StarforgeApp.h` (`SampleInfo`, the `ListSamples` declaration
`:227-232`, `m_NewProjectKind` `:376`); `AP03AuthoringSelfTest.cpp` (the E06 block); `tests/CMakeLists.txt`
(the test row); the KI register (append).

## Scope

- **In:** items 20 and 4; LH01–LH02.
- **Out:** the SDK zip contents (UX-04 decides whether `Projects/*` ships); the prefs file (UX-02);
  renaming or moving any fixture DLL or its output directory; changing how tests load fixtures.

## Deliverables

`CS_TEST_FIXTURE()` and its nine uses; the launcher skip with its log lines; grouped samples with
PendulumLab featured and its thumbnail; the welcome and the App default; `test_launcher_scan.cpp`; the
wrapper; the extended E06; `ux03-launcher` manifest; the KI registered and closed;
`evidence/UX-03/report.md`; §11 rows.

## Done when (DoD)

LH01 and LH02 pass on Debug and Release; `CosmicApp.exe` lists only real projects; every fixture-loading
manifest and the E06 old oracle still pass; both audits and the link checker exit 0.

## Rollback

Revert the lane's commits in reverse order. The marker export is inert for any host that does not look for
it, so reverting the launcher skip alone restores today's listing.
