# UX-05 — Cosmic as an SDK: `sdk` preset, app-repo kit, CI consumer job, SF_Telem dry run

**Gate:** G3 · **Wave:** 3 (worktree; alongside UX-D1 and UX-H1) · **Runs:** worktree `ux/05` at `build\_lanes\ux-05` ·
**Base:** `main` after UX-04 → UX-G0 → UX-D2 have landed (post-wave-2) · **Depends on:** UX-04 (zip, `Stage-Sdk.ps1`,
the app-repo packaging fix you only verify); UX-D1 lands before you (guide section) · **Acceptance:** EX01–EX05 ·
**Model:** Opus 5.5 · **Effort:** high · **Status:** not started

D-LIBRARY without engine changes: an app repo holds Cosmic at `extern/Cosmic` (tag-pinned submodule or the unzipped SDK),
Cosmic's own build makes the SDK via a preset, the app CMake stays the template's; generator, CI job, SF_Telem dry run.

## Copy-paste prompt

~~~text
Execute only UX-05 from the Cosmic UX & Shipping packet (docs/plans/ux-shipping-2026-09-24/). Read ONLY:
work-orders/README.md (all); 01-Contracts.md §4, §5 + your §10 row; 03-Acceptance-Catalog.md F-APPREPO + rows EX01-EX05;
CMakePresets.json; Projects/Starforge/assets/templates/app/CMakeLists.txt; Projects/SF_Telem/CMakeLists.txt;
.github/workflows/ci.yml; installer/Stage-Sdk.ps1 (UX-04's). Anything else only at a named anchor.

Lane: from C:\dev\Cosmic  git worktree add build\_lanes\ux-05 -b ux/05 <post-wave-2 main SHA> ; work only there, $env:COSMIC_SDK
= that path (EX01/EX02 remove it). UX-D1 (docs/guides, the docs/developer rename, GuideWalkthroughSelfTest.cpp) and UX-H1
run concurrently: never touch their files. Nothing under Cosmic/src or Projects/ changes except the SF_Telem banner - the
packager (Packager.cpp, StarforgeApp.cpp) is UX-04's: you verify its fix, never edit it.

Facts at 0c2edd8 (re-check every anchor first, README rule 2):
- CMakePresets.json (v3): hidden `base` pins "Visual Studio 18 2026" (:12), binaryDir ${sourceDir}/build (:14-15); `2d`
  (:20-28); no build presets. CI configures WITHOUT a preset on windows-latest (ci.yml:76-77), maybe VS 2022 only.
- Root CMakeLists.txt: default skip "AnalysisSample;PendulumLab" (:148-150); the sticky migration (:161-170) keeps any other
  stored list, so a checkout configured once with the sdk preset keeps SF_Telem skipped and tests OFF afterwards.
- BuildRunner::Start (BuildRunner.cpp:47-74) configures <project>/build with -A x64, FindCMake's cmake (:20-45) and no -G:
  build.bat must use the same cmake and binary dir or Ctrl+B meets a generator mismatch. ScaffoldProjectTo
  (StarforgeApp.cpp:297-324) copies templates/app byte for byte, replacing @PROJECT_NAME@ in every file.
- CosmicApp.exe --project <dll> mounts project:// by DLL stem under <runtime>\assets\projects\<stem> (Application.cpp:
  778-786; only the editor uses PATH mode, StarforgeApp.cpp:205/288): an App-template DLL finds no content there, so the
  app is ALWAYS run as its packaged <App>.exe from another dir, never via CosmicApp --project. SF_Telem differs: its CMake
  ignores GAME_OUTPUT_DIR and writes DLL + assets into ${COSMIC_SDK_DIR}/build/Runtime/<cfg> (SF_Telem/CMakeLists.txt:112-126).
- At 0c2edd8 packaging an app repo took its root as content (StarforgeApp.cpp:2985-2987) and wrote to extern\Cosmic\dist
  (:2970). UX-04's KI fix skips extern/dist/.gitmodules/build.bat on both sides and writes <repo>\dist\<App>: verify it
  at your base; if absent, STOP packaging the repo and report. Hosted runners have no GPU (ci.yml:9-13).

Do, each its own commit:
1. CMakePresets.json: configure preset `sdk` (inherits "2d"; COSMIC_BUILD_TESTS "OFF", COSMIC_SKIP_PROJECTS
   "AnalysisSample;PendulumLab;SF_Telem"); buildPresets `sdk-release` / `sdk-debug`. `2d` unchanged.
2. tools/Build-Sdk.ps1 [-Root <checkout>] [-Config Release|Debug|Both] [-Force] (PS 5.1, ASCII): cmake as FindCMake picks
   it; refuse a <Root>\build whose CMakeCache.txt has COSMIC_BUILD_TESTS:BOOL=ON unless -Force; configure with the preset,
   and if its generator is absent pass -G for the newest installed VS (vswhere) - verify a command-line -G overrides a
   preset in that cmake, else run the raw configure with identical cache values (record which). Assert per config the §4
   runtime set incl. assets\projects\Starforge\templates\app, no tests/fixtures/SF_Telem.dll; point Stage-Sdk.ps1's -Build at it.
3. tools/New-AppRepo.ps1 -Name <App> -Dest <dir> [-CosmicTag <commit-ish>] [-Sdk submodule|zip] [-CosmicSource <url|path>]
   [-From <project dir>] (the last two are additions, §11): git init; submodule = git submodule add <source, default
   https://github.com/kdadabhoy/Cosmic.git> extern/Cosmic (a local path needs git -c protocol.file.allow=always on that
   command) + checkout --detach <tag>; zip = no .gitmodules, README says unzip Cosmic-SDK-<ver>-win64.zip to extern\Cosmic;
   CMakeLists.txt src/ assets/ flows/ scenes/ project.cproj byte-identical to ScaffoldProjectTo (no BOM or EOL change),
   or with -From that project's CMakeLists.txt + folders; build.bat: COSMIC_SDK=%~dp0extern\Cosmic; skip the SDK build if
   extern\Cosmic\sdk.toml exists (a zip), else extern\Cosmic\tools\Build-Sdk.ps1 -Config Both; configure %~dp0build with
   -DCOSMIC_SDK_DIR -DGAME_OUTPUT_DIR=%~dp0build, build Debug + Release, no pause, exit codes propagate; .gitignore (build/,
   dist/); README.md (open in extern\Cosmic\build\Runtime\Release\Starforge.exe, Ctrl+B, Play, File > Package; zip; bump tag).
4. ci.yml job `consumer` (that block only, L4; windows-latest; fetch-depth 0): New-AppRepo -Name CiApp -Dest
   $env:RUNNER_TEMP\CiApp -Sdk submodule -CosmicSource $env:GITHUB_WORKSPACE -CosmicTag $env:GITHUB_SHA (local-path
   submodule = the path pin); build.bat (SDK via the preset, app both configs); Stage-AppPackage.ps1 -SdkRoot extern\Cosmic
   -App CiApp -RuntimeDir extern\Cosmic\build\Runtime\Release -ProjectContentDir <repo> -AppDllPath
   <repo>\build\Release\CiApp.dll; the list has K02's shape (root exe/dlls/boot.cfg, licenses/, user/README.txt,
   assets/projects/CiApp/{project.cproj,flows,scenes,assets}; nothing from extern/, src/, build/); run the PACKAGED exe -
   ENVIRONMENT_BLOCKED on the GPU-less runner, with the DLL export-table check (the three module exports, both configs)
   in its place; upload the lists.
5. Projects/SF_Telem/README.md: D-SPECIMEN banner on top - frozen test specimen (tests/CMakeLists.txt:155-163, :176 compile
   its sources; S01 drives it); the product lives in Kaden's SF_Telem repo; link docs/guides/06-package-and-ship.md.
6. After rebasing onto UX-D1: append "Your app in its own repo" under D1's placeholder in docs/guides/06-package-and-ship.md
   (submodule or zip, New-AppRepo, build.bat, Starforge, SF_Telem as the example; pictures only via tools/guide_shots.py).

Dry run (evidence/UX-05/sf-telem/; repos under <lane>\build\_temp\ux05\ like F-APPREPO - if a path exceeds MAX_PATH,
use a shorter root and name it): New-AppRepo -Name SF_Telem -From <lane>\Projects\SF_Telem (src assets firmware docs +
CMakeLists.txt) -Sdk submodule -CosmicSource <lane> -CosmicTag <lane HEAD>, committed; build.bat; package with
Stage-AppPackage.ps1 defaults (K01), diff with the in-tree K02 SF_Telem list; run the packaged SF_Telem.exe from another dir
(bounded, graceful close, log excerpt); repeat in zip mode on UX-04's zip. Record for Kaden: SF_Telem writes into the SDK
runtime dir (no GAME_OUTPUT_DIR) - adopt the template's block in his repo.

Acceptance (driver tests/acceptance/fixtures/Run-UX05Consumer.ps1, manifest tests/acceptance/manifests/ux05-consumer.manifest.json):
- EX01: F-APPREPO (submodule mode, from a CLEAN clone of the lane, in <lane>\build\_temp\ux05\): build.bat -> cmake --preset
  sdk + sdk-release/sdk-debug succeed in extern/Cosmic -> the app DLL builds both configs into <repo>\build\<cfg>. Then,
  COSMIC_SDK removed, Starforge.exe from extern\Cosmic\build\Runtime\Release (cwd = that folder, not an editor child):
  (a) X01PackageSelfTest (COSMIC_X01_PACKAGE, COSMIC_X01_PROJECT = <repo>; X01PackageSelfTest.cpp:1-14) opens <repo>,
  builds, packages to <repo>\dist\<App> with K02's shape and nothing from extern/; the packaged exe runs from another dir,
  logs the flow's start state, closes gracefully. (b) Ctrl+B + Play reaching Home: GuideWalkthroughSelfTest with
  COSMIC_GUIDE_SKIP_PACKAGE=1 from that Starforge.exe. No hook opens an existing folder and then presses Ctrl+B + Play:
  state which leg proved which claim (the orchestrator may add a computer-use screenshot).
- EX02: the same repo with UX-04's zip unzipped into extern\Cosmic, so COSMIC_SDK (build.bat) = the unzipped SDK: build.bat
  skips the SDK build (sdk.toml), the app builds both configs, its packaged exe runs.
- EX03: New-AppRepo output tree equals the documented layout (file list), no @PROJECT_NAME@ in any text file, .gitmodules
  pins the requested tag; template files byte-identical to ScaffoldProjectTo's (AP03AuthoringSelfTest.cpp:122 oracle).
- EX04: PendulumLab Y01 (ap04-sample) and E06 (ap03-editor, case AP03-EDITOR) green; git diff --stat main -- Cosmic/src
  Projects shows only Projects/SF_Telem/README.md.
- EX05: the `consumer` job, run locally exactly as CI would in a fresh temp dir; green on Kaden's push, pending until then;
  its run step ENVIRONMENT_BLOCKED there with the export-table check in its place.

Evidence: evidence/UX-05/report.md (WO-10 layout), both dry runs, lists, results.json, the generator used; §11 rows. KI
rule: a defect -> README's next free KI BEFORE any fix; a fix outside your files (the packager included) goes to the
orchestrator. Land (L2) after UX-D1: git rebase main;
Debug + Release 0-warn; CosmicTests both; ux05-consumer, ap04-sample, ap03-editor; both audits + check_docs_links.ps1 +
check_api_matrix.ps1; commit on ux/05 as kdadabhoy <kdadabhoy28@gmail.com>, no Co-Authored-By / AI trailer; never push.
Report <= 40 lines: EX verdicts, generator used, dry-run list diffs, deviations, SHAs, git status --short.
~~~

## Files to read first (and nothing else)

The eight in the prompt: README; §4/§5 + §10 row; EX rows; presets, App template CMake, SF_Telem CMake, `ci.yml`, stager.

## Owns / May touch

- **Owns:** `CMakePresets.json`; `tools/Build-Sdk.ps1`, `tools/New-AppRepo.ps1` (new); the `consumer` job in `ci.yml`;
  `Projects/SF_Telem/README.md` (banner); `Run-UX05Consumer.ps1`; `manifests/ux05-*.json`; `evidence/UX-05/**`.
- **May touch:** `docs/guides/06-package-and-ship.md` (appended section, after D1); `installer/Stage-Sdk.ps1` (`-Build`
  block); `01-Contracts.md` §11 rows; the KI register. Never `Packager.cpp` / `StarforgeApp.cpp` (UX-04's fix, verified here).

## Scope

- **In:** preset, wrapper, generator, CI job, dry runs (submodule + zip), specimen banner, the guide section.
- **Out:** engine/editor/template changes; moving or deleting `Projects/SF_Telem`; creating Kaden's repo; any push.

## Deliverables

Three presets, `Build-Sdk.ps1`, `New-AppRepo.ps1`, `consumer` job, banner, guide section, driver + manifest, report + dry runs.

## Done when (DoD)

EX01–EX04 green locally with every leg named, EX05 pending on Kaden's push; a clean clone builds the SDK with the preset;
the generated repo builds, packages and runs on the submodule and on the zip; in-tree builds unchanged.

## Rollback

Revert the lane's commits; preset, scripts and CI job are additive, the banner is text only.
