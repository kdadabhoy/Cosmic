# UX & Shipping — acceptance catalog

Status: test specification, 2026-09-24. No case here has been executed. The execution model, tiers
(U unit / W in-process host / I editor self-test host / G GPU / Q qualification), profiles (`pr`, `release`,
`gpu`, `nightly`), evidence contract, isolation rules, deadlines and numeric bars are those of
[`../2d-stability-2026-09-16/03-Acceptance-Test-Catalog.md`](../2d-stability-2026-09-16/03-Acceptance-Test-Catalog.md)
and are not restated. Runner: `tests/acceptance/Run-Acceptance.ps1` with per-WO manifests
`ux<wo>-<group>.manifest.json`.

Retained unchanged from the earlier packets: every unit/render suite expectation (CosmicTests ≥ 523 / 0
failed / 14 skipped in both configs; CosmicRenderTests 45 with the 15 goldens byte-identical), B06, E06, E08,
K01–K04, Y01–Y03, S01–S04, DOC01–DOC03, DOC05 and every `ap*` / `wo*` manifest. A case that a lane extends
(E06, E08, K02) keeps its ID and its old oracle; the extension is listed under the new ID.

## Fixtures (in addition to the earlier packets')

| Fixture | Definition |
| --- | --- |
| F-FLOWS | Every tracked `.cflow` on `main` (templates, samples, PendulumLab, test fixtures) hashed before UX-01; opened and saved through the FlowEditor document path after UX-01 — the hashes must match. |
| F-SDK | The zip produced by `Stage-Sdk.ps1` from the lane's Release build, unzipped into `build\_temp\ux04\sdk\`; its sorted file list is committed as `evidence/UX-04/sdk-files.txt` and is the K02-style oracle for later re-stagings. |
| F-APPREPO | An app repo generated by `tools/New-AppRepo.ps1` in `build\_temp\ux05\`; submodule mode uses a path pin to the lane checkout instead of a network clone. |
| F-GUIDESHOTS | The raw driver shots + `annotations.json` for every guide image, under the lane's `build\_temp\ux-d1\shots\`; `docs/guides/images/manifest.json` names each image's source shot. |

## VM enablement (UX-V0)

The campaign VM (2026-09-25) has only `OpenGL 4.5 — llvmpipe (LLVM 13.0.1)`, VMware's Mesa software renderer — a
strict GLSL compiler. CosmicRenderTests golden comparisons are not authoritative there (llvmpipe rasterises slightly
differently): they are run and counted, never used to loosen a tolerance or regenerate a golden; the real-GPU run is
HOST-VERIFY (`evidence/UX-V0/HOST-VERIFY.md`).

| ID | Tier/profile | Procedure and oracle |
| --- | --- | --- |
| VM01 | U,G/pr | **Conformant shaders** (KI-82). `tests/check_gl_conformance.ps1`'s second pass scans every `*.glsl` in the repo (directories `.git`, `build`, `out`, `dependencies`, `vendor`, `extern`, `third_party`, `node_modules` exempt) and exits 1 when a `uniform sampler*` array is indexed by anything but an integer literal, naming `file:line`; the GL-token pass is unchanged. Oracle: exit 1 on the unfixed tree listing `Texture.glsl`, `QuadInstance.glsl` and the two `wo08_tint_quad*` fixtures (failing-before, kept) and exit 0 after. And on the VM (llvmpipe), Debug and Release: the logs of a `Starforge.exe` start, a `CosmicApp.exe` start and a full `CosmicRenderTests.exe` run contain no `Shader compilation failure`, `Shader link failure` or `Shader::Create:` error line other than the VM02 fixture's own (each of those names `ux_v0_`; counted by `evidence/UX-V0/scan_shader_errors.py`). On a real GPU the batch-shader rewrite is output-identical: the 15 goldens pass unchanged (HOST-VERIFY). |
| VM02 | G/gpu | **No crash on a failed shader** (KI-83). `tests/render/render_ux_v0_shader_failure.cpp` (suite `UX-V0 VM02`) with the real broken fixture `tests/render/fixtures/ux_v0/ux_v0_broken.glsl` through the production loader only: (1) `Shader::Create(<fixture>)` returns nullptr, `Shader::GetLastCreateError()` and exactly one captured error line name the path, a non-empty `renderer '…'` and `OpenGL '…'` and the compiler's first error line (`ux_v0_undeclared`); a missing file reports `could not read the file`; (2) with `COSMIC_SHADER_OVERRIDE=Texture.glsl=<fixture>`, `Renderer2D::Init` returns false with the reason in `GetInitError()` and a frame of quads + a line + a circle runs without a crash (the quad pixels keep the clear colour); with `Line.glsl` + `Circle.glsl` mapped instead, `Init` succeeds, the quad draws and the line and the circle are skipped; the harness renderer is rebuilt and a control frame draws all three; (3) `CosmicApp.exe` started with the Texture override (stdout/stderr to a file, `COSMIC_NO_FATAL_DIALOG=1`) exits within 120 s with `Application::StartupFailureExitCode` (2), never `0xC0000005`, and its output says `could not start` with `Texture.glsl`, `ux_v0_undeclared` and the renderer string. Passes Debug and Release (on the VM: authoritative — no golden involved). Failing-before: the unfixed Starforge / CosmicRenderTests Release exit `0xC0000005` (kept). |
| VM03 | I/release | **The editor runs in the VM.** `Starforge.exe` (Debug and Release) starts under llvmpipe and is still running after 60 s with no shader error in its log; the retained editor manifests `tests\acceptance\manifests\wo07-l05.manifest.json` and `ap03-editor.manifest.json` PASS in Debug and Release (`Run-Acceptance.ps1` with an absolute `-Manifest` and `-TempRoot <worktree>\build\_temp\<name>`; a failure under load is re-run alone once before it counts). Failing-before: the Release editor exits `0xC0000005` at start-up (kept). |

## Flow editor (UX-01)

| ID | Tier/profile | Procedure and oracle |
| --- | --- | --- |
| FE01 | U/pr | `NodeCanvas::RouteLink` unit: forward link (target right of source), backward link (target left, same row), backward link (target left and above), self-loop, vertical stack (target directly below) — for each, the cubic bezier sampled at 64 points never enters the source node rect (expanded by 2 px) except at its own pin, and for forward links the control points equal today's defaults (regression). Bit-exact assertions on the control points for the five cases. |
| FE02 | U,I/release | F-FLOWS byte-stability; and a headless `FlowAsset` round-trip of a transition driven through the trigger-kind selector's command path Event → When → Event: the final asset equals the original field-for-field with no `HasGuard` and no `if` block; When without a guard is refused by `Validate`; an empty guard on any transition is reported by `Validate`. |
| FE03 | U,I/release | Headless `AssetEditorHost` test: `Open()` sets visible; setting `*open=false` hides the window on the next frame while a document is open; a later `Open()` shows it again; `CloseAll()` empties the host. Self-test: with `Main.cflow` open, an injected click on the Editors tab's ✕ hides the dock (window absent from ImGui's window list next frame); `CloseProject` leaves `AnyOpen() == false`. |
| FE04 | I/release | Self-test: fresh editor prefs, open PendulumLab, Screens ▸ Flow graph → next frame the "Editors" window is focused, its size ≥ 1100×680 or docked at ≥ 800×400 canvas, and every state node's rect lies inside the canvas rect (`CenterOnContent` applied); screenshot saved. |
| FE05 | U/pr | A flow created by `CreateDefaultAsset` and opened in a `FlowEditor` document reports `Dirty() == false` after two frames; closing and reopening a second document keeps the first document's tab id (stable ids); existing `test_flowmachine.cpp` EditorPos round-trip unchanged. |

## Viewport, hierarchy, inspector, scenes, preferences (UX-02)

| ID | Tier/profile | Procedure and oracle |
| --- | --- | --- |
| ED01 | U,I/release | Unit: `Gizmo::Manipulate` in 2D mode with a 30° rotation delta writes `Rotation.z` = 30° ± 1e-4 and leaves `UseQuatRotation == false`, `RotationQuat` untouched. Self-test: in the App template's Dashboard, select the Canvas's `Plot` element → no transform gizmo drawn (probe counter), the rect gizmo drawn; select a world sprite (a scene with one `SpriteRenderer` under an opaque `UiImage`) → an injected press-drag of (40, −20) px starting on the sprite moves `Transform.Position` by the world equivalent with exactly one `CommandStack` entry; undo restores it. |
| ED02 | U/pr | Headless `UiSystem`: a canvas with a `UiPlot` child whose `TagComponent::Active = false` — `Render` records no draw for it (draw-list probe), `Update` with a pointer over it returns false and emits nothing, `HitTest` skips it, `CollectHostedPanels` omits a hosted panel under it; re-activating restores all four. Beside `tests/test_ui_widgets.cpp:536-548`. |
| ED03 | U,I/release | `SourceLocator::ForSignal("back_clicked")` on PendulumLab returns one `Flow` hit per matching transition (`Settings —back_clicked→ Lab`, plus any `src/` hits) with state/target filled; E08's existing cases unchanged. Self-test: the Inspector on the Settings Back button shows the `Flow:` line; "Open in flow editor" opens `Main.cflow` with that transition selected (`m_SelectedLink` probe). |
| ED04 | U,I/release | Screens ▸ Scenes lists exactly the set `project://scenes/**/*.cscene` (a fixture project with a nested `scenes/overlays/Pause.cscene` and one `.cscene.bak` that must not appear), the start scene marked; double-click opens (`ctx.Scene` path probe); File ▸ Open Scene shows the same set. |
| ED05 | U,I/release | Prefs: `editor.toml` round-trip of `autosave_enabled`, the interval and `prompt_unsaved` (write → reload → equal; defaults when absent). Self-test: with `autosave_enabled=false` and a dirty scene, 2× the interval elapses (injected clock) and no autosave file appears; with it on, one appears and the status bar shows `autosaved`; with `prompt_unsaved=true` and a dirty scene, `OpenScene` shows the Save/Discard/Cancel modal (window present), Cancel keeps the scene, Discard switches; with `prompt_unsaved=false` it switches directly; a dirty scene at `OnDetach` leaves an autosave copy (the window-close path). |

## Launcher and samples (UX-03)

| ID | Tier/profile | Procedure and oracle |
| --- | --- | --- |
| LH01 | U,W/pr | Unit: `LauncherLayer::ScanForProjects` over a temp dir holding a copy of a real plugin DLL and a copy of `AP01ServiceFixture.dll` lists exactly the plugin; every `tests/*Fixture.cpp` DLL exports `CosmicTestFixture` (GetProcAddress probe over the runtime dir); `WO07NoExport.dll` still hidden. W: `CosmicApp.exe` started with no arguments lists `Starforge` and `SF_Telem` and no `*Fixture` name (launcher list probe via the existing env-gated launcher snapshot or a log line). |
| LH02 | I/release | E06 extended: the samples list contains PendulumLab (featured, first, group "App samples") and FlowDemo/ForgePong (group "Game samples"); each scaffolds to `Documents/Starforge Projects/<Name>`, opens, builds and plays; the welcome popup names PendulumLab; the New Project dialog's default template is App; `Prefs::LoadProjects()` call count over 600 homescreen frames ≤ 3 (unchanged). |

## SDK release (UX-04)

| ID | Tier/profile | Procedure and oracle |
| --- | --- | --- |
| SD01 | I,Q/release | Script `Run-UX04Sdk.ps1`: stage the zip from the lane's Release build → unzip to `build\_temp\ux04\sdk` → remove `COSMIC_SDK` from the environment → start `<sdk>\build\Runtime\Release\Starforge.exe` with cwd = its folder → through the existing env-gated self-test hooks: New Project (App) at `build\_temp\ux04\projects\SdkApp` → `BuildScripts` succeeds (the project configured with `-DCOSMIC_SDK_DIR=<sdk>`) → Play reaches `Home` → Package → the packaged exe started from `build\_temp\ux04\elsewhere` writes its log and answers the Y02-style self-test signal. Exit 0. Both the zip and the editor log are excerpted. |
| SD02 | W/pr | Plain cmake: `cmake -S <app template copy> -B build\_temp\ux04\plain -A x64 -DCOSMIC_SDK_DIR=<sdk> -DGAME_OUTPUT_DIR=…` and build Release and Debug against the unzipped SDK → both DLLs produced; loading each through `CosmicApp.exe --project` from the SDK's runtime dir prints the module's registration line. |
| SD03 | U/pr | The zip's sorted file list equals `evidence/UX-04/sdk-files.txt` (committed once by this WO); contains no `*.pdb`, `*.exp`, `CosmicTests*`, `*Fixture*`, `WO07NoExport*`, `SF_Telem*`, `CMakeCache.txt`, `assets/cache/**`; contains `Projects/PendulumLab/project.cproj`, every LICENSE path `installer/licenses/MANIFEST.txt` names, and the CRT DLLs; `sdk.toml` `sha` equals the lane's HEAD; zip size recorded (not gating). |
| SD04 | Q/release | `Starforge-Setup-<ver>.exe` run on a disposable Windows 10/11 VM without Visual Studio: installs, the Start-menu shortcut opens the homescreen, New Project reports the missing toolchain honestly (Console line), uninstall leaves no `{autopf}\Cosmic SDK`. `ENVIRONMENT_BLOCKED` with the prerequisite named when no VM is available — never a pass. |
| K02-bak | W/pr | K02 with a `scenes/Main.cscene.bak` in the fixture project: the file lists from the editor packager and `Stage-AppPackage.ps1` are identical and contain no `.bak`; the pre-fix run is kept as failing-before. Same procedure for an F-APPREPO-shaped project (`extern/`, `dist/`, `.gitmodules`, `build.bat` present): nothing from those paths is staged and `dist/` lands under the project (the app-repo packaging KI). |
| SD05 | W/pr | **VC++ runtime present** (D-TOOLCHAIN; the KI UX-04 registers before fixing). `tests/acceptance/fixtures/Test-PackageImports.ps1 -Root <dir>` (PS 5.1, ASCII) finds `dumpbin.exe` through vswhere (`-latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath`, then `VC\Tools\MSVC\<ver>\bin\Hostx64\x64\`), runs `dumpbin /dependents` on every `*.exe`/`*.dll` under the root, follows the package's own DLLs transitively, and classifies each import as **package** (beside the importing binary), **system** (an `api-ms-win-*`/`ext-ms-*` API set, or present in `%SystemRoot%\System32` **and not** a Visual C++ Redistributable file — `vcruntime*`, `msvcp*`, `concrt*`, `vccorlib*`, `vcomp*`, `mfc*` — because the dev machine has the redistributable installed system-wide and a plain System32 check would hide the defect) or **MISSING**; an import of a debug CRT (`*d.dll`, `ucrtbased.dll`) is MISSING. Oracle: zero MISSING in (1) a `Stage-AppPackage.ps1` package of PendulumLab (the path `package.bat <App>` and `release.yml`'s `package` job use), (2) the editor Packager's package of the same project (X01/Y02 path), (3) the staged SDK's `build\Runtime\Release` (Debug excluded: the debug CRT is not redistributable — recorded); and (4) every installer script (`AppSetup.iss`, `CosmicSetup.iss`, `StarforgeSetup.iss`, the Packager-generated `.iss`) sources a directory checked above or runs `vc_redist.x64.exe` (grep of `[Files]`/`[Run]`). If UX-04 chooses the `vc_redist.x64.exe` route, a runtime import counts as satisfied by `vc_redist.x64.exe` in the package plus the installer's `[Run]` entry, and the portable `dist` folder documents that it needs the redistributable. Version leg: the packaged `msvcp140.dll` file version ≥ the MSVC toolset of every binary in the package (the engine's from `sdk.toml` `msvc_version`, the project DLL's from its `CMakeCache.txt` `CMAKE_CXX_COMPILER_VERSION`). `sdk.toml` carries `toolchain = "msvc"`. Failing-before: leg (1) on the unfixed tree lists the VC++ runtime DLLs as MISSING — kept. `docs/installer-guide.md` claims bundling only where it happens (its `:37` and `:99` claims corrected). Runs on any machine with Visual Studio (the dev machine, CI) — no clean VM needed. |

## Consumer path (UX-05)

| ID | Tier/profile | Procedure and oracle |
| --- | --- | --- |
| EX01 | W,I/release | F-APPREPO (submodule mode): `build.bat` → `cmake --preset sdk` + `sdk-release`/`sdk-debug` succeed in `extern/Cosmic` → the app DLL builds both configs into `<repo>/build/<cfg>` → `Starforge.exe` from `extern/Cosmic/build/Runtime/Release`, driven by the existing self-test hooks (the package self-test for open + build + package, the guide self-test for Ctrl+B + Play; the report says which leg proved which claim), opens `<repo>`, `BuildScripts` succeeds, Play reaches `Home`, Package produces `<repo>/dist/<App>` with K02's file-list shape and nothing from `extern/` in it → the packaged exe runs from another directory (`CosmicApp.exe --project` is not used: it cannot find an external app's content). |
| EX02 | W/release | The same repo with `COSMIC_SDK` = the unzipped UX-04 SDK instead of the submodule: app builds both configs and runs. |
| EX03 | U/pr | `New-AppRepo.ps1` output tree equals the documented layout (file list), `@PROJECT_NAME@` absent from every text file, `.gitmodules` pins the requested tag. |
| EX04 | U,W/pr | Nothing moved for in-tree users: PendulumLab Y01 standalone build and E06 re-run green; `git diff --stat main -- Cosmic/src Projects` shows only `Projects/SF_Telem/README.md`. |
| EX05 | Q/pr | The `consumer` CI job is green on Kaden's push (reported as pending until then); its run step is `ENVIRONMENT_BLOCKED` on the GPU-less runner with the DLL export-table check in its place. |

## World contract (UX-G0)

| ID | Tier/profile | Procedure and oracle |
| --- | --- | --- |
| WM01 | U/pr | `ProjectManifest` load → save round-trip keeps an unknown key (`capture_cursor = true`), an unknown table (`[world] pixels_per_unit = 32`) and comments, and an unchanged manifest (the App template's and PendulumLab's) round-trips byte-identical; Project Settings ▸ Save on an app project keeps `kind = "app"`, `startup_flow` and `pixel_art` (failing-before kept); `SceneSerializer::LoadFromString` with `cosmic_scene: 2` warns exactly once and loads; `PlayerLayer::OnAttach` order probe: a fixture service's `OnAttach` runs before `PhysicsWorld::Init` (recorded sequence); the 2D clear colour constant read from both hosts is identical (one symbol). |
| WM02 | U,G/gpu | Today's App/Game/Blank templates, FlowDemo, ForgePong and PendulumLab load and save byte-identical through the new code (`SceneSerializer`, `ProjectManifest`); all 15 goldens byte-identical; `ap02-gpu` and `wo08-gpu` unchanged. |
| WM03 | I/release | The design doc's example scene (`04-World-Mode-Design.md`: sprites at three Z depths under a perspective camera, a HUD canvas, a `SystemScript` orbit integrator at the fixed step) is committed as a fixture project, opens in the editor, plays for 600 frames with the orbit radius within 1e-3 of the analytic value at every fixed step, and the editor Play frame shows the post chain applied (readback: bloom sentinel pixel differs from the no-environment frame). Screenshot saved. |

## Guides (UX-D1; DG02 executed by UX-Q1)

| ID | Tier/profile | Procedure and oracle |
| --- | --- | --- |
| DG01 | U/pr | `docs/guides/images/manifest.json` lists every PNG under `docs/guides/images/**` and nothing else; every entry's `source_shot` exists in the lane's evidence (hash recorded) and `annotated_by` is `guide_shots.py`; every image ≤ 600 KB and ≤ 1440 px wide; every guide's `![...](images/...)` reference resolves (link checker) and is in the manifest; no image is referenced by two steps. |
| DG02 | Q/release | From the UX-04 zip in a temp dir, following only guides 00 and 01 (no repo checkout, no `COSMIC_SDK`): the driver reproduces every step of guide 01 against the zip's `Starforge.exe` and the exported exe runs from another directory (the `Run-GuideWalkthrough.ps1` oracle); the log of where the guide text was unclear is fixed in the guide, not the log. |
| DG03 | U/pr | **Install step** (D-TOOLCHAIN). `evidence/UX-D1/check_guide00_toolchain.py` exits 0: guide 00 has a numbered step titled `Install the C++ compiler` before its first step that builds anything; it names Visual Studio Community 2026 and the Build Tools for Visual Studio 2026 (product names as Microsoft writes them), the workload "Desktop development with C++", and both winget one-liners byte-equal to the H1-E strings; `README.md`'s prerequisites name the Build Tools as the lighter alternative and its cmake-path lines (`README.md:37`, `:59` at `60a0536`) no longer imply Community is the only edition; the step's pictures are in the images manifest per DG01 (the Visual Studio Installer workload page by computer-use, nothing modified; the compiler-check message added by UX-H1 at its landing — until then a named exception). UX-Q1 re-runs it with one more leg: guide 00's one-liners equal the strings in the landed H1-E source (grep). |

## API matrix (UX-D2)

| ID | Tier/profile | Procedure and oracle |
| --- | --- | --- |
| DM01 | U/pr | `tests/check_api_matrix.ps1` exits 0: every row's header exists under `Cosmic/src/` and contains the call's identifier; every `path:line` example exists and the line is inside the file; the matrix covers every header in the reference manifest (a header with no row is reported). |
| DM02 | U/pr | Every inline example compiles: the snippets are extracted into `tests/test_api_matrix_examples.cpp` (generated by a script committed with the WO) and built into CosmicTests (no runtime assertions beyond compiling and, where cheap, a smoke call). |

## Hardening carry-over (UX-H1)

| ID | Tier/profile | Procedure and oracle |
| --- | --- | --- |
| H1-A | W,I/nightly | Ground-control dry run: an `AppService` on `SerialLink` over `FakeSerialTransport` feeding 8 channels at 50 Hz into `UiPlot`/`UiGauge`/`UiValueText` for 60 min under a job object: no frame > 100 ms, memory plateau (WO-02 oracle), plot decimation ≤ 512 segments. |
| H1-B | W/nightly | Scale profile: 200 channels, 6 plots at full `WindowSeconds`, frame time Debug/Release recorded (not gating), regression bar set for UX-Q1. |
| H1-C | I/release | `kind = app` New Project: either a build starts once after scaffold (Console line within 5 s) or the Live chip's tooltip says `press Ctrl+B`; E01 unchanged. |
| H1-D | W/pr | `CrashDump::Install` + a deliberate access violation in a fixture host process writes `user://logs/*.dmp` (file > 0 bytes, `MiniDump` signature) and the log names it; both `CosmicApp.exe` and `Starforge.exe` install it (probe log line at boot). |
| H1-E | I/release | **Compiler check** (D-TOOLCHAIN; KI first). The toolset probe beside `BuildRunner::FindCMake` answers "MSVC present" only when vswhere (`%ProgramFiles(x86)%\Microsoft Visual Studio\Installer\vswhere.exe -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath`, the `package.bat:59` query) returns an installation **and** a cmake exists (FindCMake's VS-bundled hit, or `cmake` resolvable on PATH). `AP03AuthoringSelfTest` plan `COSMIC_AP03_PLAN=toolchain`, run twice by `Run-UXH1Toolchain.ps1` / `ux-h1-toolchain.manifest.json`: (a) with `COSMIC_SIMULATE_NO_TOOLSET=1` — read only inside the probe, where it makes vswhere's answer and FindCMake's VS search come back empty; everything downstream is the production path — the homescreen's first frame shows the `C++ compiler not found` message (window probe); New Project (App) scaffolds but starts no build; `BuildScripts` (Ctrl+B) and File ▸ Package start none either (`BuildRunner` stays `Idle`, no `cmake.exe` child process, nothing new under `dist/`), each showing the message and exactly one Console line naming the probe result; the message contains both strings below byte-equal and the guide-00 pointer; a screenshot is saved (the source shot of guide 00's picture). (b) Without the override on the dev machine: the probe reports the installation and cmake paths (recorded), no message, and a New Project build still succeeds. Failing-before: plan (a) against a binary without the check (a build starts, no message) — kept. Strings (verbatim): `winget install -e --id Microsoft.VisualStudio.BuildTools --override "--passive --wait --add Microsoft.VisualStudio.Workload.VCTools --includeRecommended"` and `winget install -e --id Microsoft.VisualStudio.Community --override "--passive --wait --add Microsoft.VisualStudio.Workload.NativeDesktop --includeRecommended"`; pointer `https://github.com/kdadabhoy/Cosmic/blob/main/docs/guides/00-get-starforge.md` and `README-SDK.md` (the SDK zip carries no `docs/`). |
| H1-F | U,W/pr | `tests/acceptance/fixtures/Run-AP04Sample.ps1`'s default `-CMake` (`:21`, a hard-coded `...\18\Community\...` path) resolves through the H1-E vswhere query + `Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe`, then PATH (today's `:30` fallback). Oracle: `git grep -n -F "18\Community" -- "*.ps1" "*.psm1" "*.bat" "*.cpp" "*.h" "*.cmake" "*CMakeLists.txt" "*.yml" "*.json"` prints nothing (Markdown mentions are UX-D1's); `ap04-sample` re-run green with no `-CMake` argument. |
| B06 | U,W/pr | Re-run per tidy commit (unchanged oracle). |

## Qualification (UX-Q1)

Everything above re-run at the pinned SHA; the retained suites; the S01 fake-clock leg (`wo06 -Profile pr`); S03 ×5 (the multi-hour soaks Y03 / S01-native /
S02 / N02-drift-2h / T05 are postponed to [`../TESTING-PLAN.md`](../TESTING-PLAN.md), D-SOAKS); K02 for PendulumLab and for the UX-05 app repo; DG02; DOC01–DOC03;
the goldens hashed before and after every landed merge (`evidence/UX-Q1/golden-hashes.txt`); the release
report with the requirement → case → evidence matrix and the staged push / tag / release commands.
The D-TOOLCHAIN additions (2026-09-25) are covered the same way: SD05 on the zip staged at the pinned SHA and on the
K02 packages; H1-E and H1-F through `ux-h1-toolchain` / `ap04-sample`; DG03 with its source-equality leg; where a VM
exists, SD04's "missing toolchain" report is H1-E's message. The release report states D-TOOLCHAIN (MSVC is the only
supported toolchain; the llvm-mingw flavour is planned in [`../TOOLCHAIN-PLAN.md`](../TOOLCHAIN-PLAN.md), not built).
