// Cameras essentially allow us to work with World coordinates
// instead of pixel coordinates
// Projection and view matricies (rotation & translation + other stuff)
// makes the screen follow/move how we want it to (animates basically)